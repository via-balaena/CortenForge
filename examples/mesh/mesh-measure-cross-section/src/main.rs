//! mesh-measure-cross-section — planar slicing of a closed cylinder.
//!
//! Spec: `mesh/MESH_V1_EXAMPLES_SCOPE.md` §5.2. Skeleton commit per
//! §6.2 #5 — the 32-segment closed-cylinder fixture builder + cross-
//! section anchors (mid-slice + slice-stack + helpers + out-of-mesh +
//! plane-normal normalization) land in §6.2 #6.

fn main() {
    println!(
        "example-mesh-mesh-measure-cross-section: skeleton (§6.2 #5). \
         Fixture + cross-section anchors land in §6.2 #6."
    );
}
