//! mesh-measure-bounding-box — AABB + OBB on a two-cube fixture.
//!
//! Spec: `mesh/MESH_V1_EXAMPLES_SCOPE.md` §5.1. Skeleton commit per
//! §6.2 #2 — fixture + anchors land in subsequent commits (§6.2 #3
//! hand-authored two-cube fixture, §6.2 #4 AABB + OBB anchors).

fn main() {
    println!(
        "example-mesh-mesh-measure-bounding-box: skeleton (§6.2 #2). \
         Fixture + anchors land in §6.2 #3 + #4."
    );
}
