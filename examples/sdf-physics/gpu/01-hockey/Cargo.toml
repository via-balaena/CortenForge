[package]
name = "example-sdf-gpu-01-hockey"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

# Headless demo — a `todo!()` scaffold for the GPU baby-step ladder
# (gpu/00-freefall … 07-strike); no oracle asserts. Classified `demo`
# so it's compile-only in CI (the `todo!()` never runs); it demonstrates
# planned architecture, it does not validate. Not run by `xtask run-validators`.
[package.metadata.cortenforge]
example_kind = "demo"

[dependencies]
# Dependencies will be added when the baby-step ladder reaches this example.

[lints]
workspace = true
