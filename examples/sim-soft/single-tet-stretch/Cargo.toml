[package]
name = "example-sim-soft-single-tet-stretch"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
# SoftScene::one_tet_cube + BoundaryConditions + LoadAxis + SkeletonSolver
# + CpuNewtonSolver + SolverConfig + Tet4 + NullContact + Solver trait
sim-soft = { workspace = true }
# Tape + Tensor for the Stage-1 theta_var registration.
sim-ml-chassis = { workspace = true }
# Result<()> return type for main(); error context wrapping for the JSON
# emit step.
anyhow = { workspace = true }
# JSON emit for out/force_stretch.json — single-record schema documented
# inline in src/main.rs::save_force_stretch_json.
serde_json = { workspace = true }

[lints]
workspace = true
