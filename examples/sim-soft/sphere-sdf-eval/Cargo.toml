[package]
name = "example-sim-soft-sphere-sdf-eval"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
# SphereSdf + Sdf trait + Vec3 (= nalgebra::Vector3<f64>)
sim-soft = { workspace = true }
# Point3 + IndexedMesh + AttributedMesh wrap for the bulk-grid PLY
mesh-types = { workspace = true }
# save_ply_attributed for out/sdf_grid.ply
mesh-io = { workspace = true }
# Result<()> return type for main()
anyhow = { workspace = true }
# assert_relative_eq! for grad-norm + off-axis-surface anchors
approx = { workspace = true }

[lints]
workspace = true
