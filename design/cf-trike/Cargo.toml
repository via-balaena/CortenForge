[package]
name = "cf-trike"
description = "One reverse trike as a cf-design Mechanism — a triangulated tube frame, a reclined seat frame, direct steering, a swingarm, three wheels and a rider, double wishbones on bushed pivots, welded into fourteen degrees of freedom, with the closed-form volume and grid cell each part needs to be weighed"
keywords = ["vehicle", "trike", "geometry", "assembly", "design"]
categories = ["science", "simulation"]
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true

[package.metadata.cortenforge]
tier = "L0"

[dependencies]
# `Mechanism`, `Part`, `JointDef`, `Solid` — the assembly this crate builds.
cf-design = { workspace = true }
nalgebra = { workspace = true }
# `Result` for the one fallible step, composing disjoint pieces.
anyhow = { workspace = true }

[lints]
workspace = true
