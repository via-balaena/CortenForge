# sim-soft Example Inventory + Plan

**Status:** SEED — to iterate across sessions before execution.

**Purpose:** surface what sim-soft can do, what examples exist (user-facing vs internal fixture vs missing), and what gaps stand between today's platform state and the workflow we actually need. Living document — delete after the cleanup arc completes per `feedback_code_speaks`.

---

## Scope

- Examples whose primary subject is sim-soft simulation.
- Includes inputs from cf-design (design surface) and `mesh-sdf` (scan import) since both are part of the user-facing workflow.
- Excludes pure-cf-design or pure-mesh examples (those would be separate inventories if ever needed).

---

## Anchoring engineering goal

The layered silicone device cavity-fit workflow per `project_layered_silicone_device` memory memo. Concretely:

- 3-layer poured silicone body (Ecoflex 00-30 / Dragon Skin 00-30 class).
- Cavity is **custom-fit to a literal scanned reference geometry**. Scan flows: `mesh-sdf::MeshSdf` → `cf-design::Solid` → `sim-soft::Sdf` → BCC + Isosurface Stuffing → tet mesh → FEM. **No parametric approximation of the scan** — user rejected that as "feels like calipers."
- Rigid indenter at sim time is the same scanned SDF, wrapped as a `RigidSdfPrimitive`.
- Readout: stretch / contact pressure / force-displacement at the cavity wall.
- Calibration: post-iter-1 cast, fit effective modulus to absorb ν=0.40 locking error (Fork B per session 2026-05-04 — predictive Phase H is parked).

The inventory should still catalog examples that are NOT directly tied to this device — the platform's general capabilities matter — but the anchoring goal sets the priority ordering.

---

## Status legend

- `have` — user-facing example exists in `examples/` (repo root) or `sim/L0/soft/examples/`.
- `internal-fixture` — capability is exercised in `src/readout/scene.rs` or under `tests/`, but no user-facing demo.
- `missing` — no demo of any kind.
- `blocked` — example would require a platform gap to be filled first.

---

## Inventory table

Organized by capability tier (museum-tour readability). The silicone-device synthesis examples sit at the bottom — they are gated on bridge work that earlier rows do *not* require, so reading top-to-bottom is also a rough dependency-chain. Priority for *which PR each row ships in* is set by §Q5 below, not by table order.

Status legend per top of file: `have` / `internal-fixture` / `missing` / `blocked`. The `Capability shown` column names the *one* concept the example is the canonical visual for. The `Gap to land` column names the platform work + wrap work needed.

### Tier 1 — SDF primitives + meshing (Phase 3 surface)

> **Standardization (2026-07-14):** rows 1–3 folded into one headless
> validator `example-sdf-stress-test` at `examples/sim-soft/sdf/stress-test/`
> (modules `sphere_eval` / `hollow_shell` / `sdf_to_tet`) — one domain → one
> stress-test, per the validation-consistency arc. Row numbers preserved as
> immutable IDs; the standalone `sphere-sdf-eval` / `hollow-shell-sdf` /
> `sdf-to-tet-sphere` crates no longer exist.

> **Standardization (2026-07-14):** row 4 (`single-tet-stretch`) folded into
> one headless validator `example-stretch-stress-test` at
> `examples/sim-soft/stretch/stress-test/` (module `single_tet`), together with
> Tier 2's rows 5–6 (`neo-hookean-uniaxial` / `multi-element-stretch`) — one
> uniaxial-stretch domain → one stress-test (the domain straddles Tier 1 / Tier
> 2). Row numbers preserved as immutable IDs; the standalone `single-tet-stretch`
> crate no longer exists.

| # | Concept | Simplest example | Capability shown | Status | Gap to land |
|---|---|---|---|---|---|
| 1 | SDF primitive eval + grad | `sphere-sdf-eval` — sample `SphereSdf::eval` + `grad` over a grid; emit signed-distance PLY (vertex extras like mesh-sdf-distance-query) | `Sdf` trait contract; analytic distance + unit gradient on the zero set | `have` (PR #226 cf-viewer arc) | — |
| 2 | Boolean SDF composition (difference) | `hollow-shell-sdf` — `SphereSdf{R_outer} \ SphereSdf{R_cavity}` via `DifferenceSdf`; emit 2D slice cross-section PLY at z=0 plane | sharp-CSG difference for hollow bodies (book Part 7 §00 §01) | `have` (`1578ae50`) | — |
| 3 | SDF → tet meshing (single material) | `sdf-to-tet-sphere` — solid `SphereSdf` → BCC + Isosurface Stuffing tet mesh via `SdfMeshedTetMesh`; emit boundary-face PLY | Phase 3 SIGGRAPH-2007 stuffing pipeline; tet-quality bounds | `have` (`aec2d07a`) | — |
| 4 | Hand-built tet (skeleton path) | `single-tet-stretch` — `SoftScene::one_tet_cube` + uniaxial load via `BoundaryConditions`; drives `Solver::step`, prints the `v_3` displacement readout + emits the full DOF trace (JSON). Self-gates only that the solve converged (Rule-B de-frag); the scene's physical correctness — `x_final` bit-equality, Dirichlet pins, displacement sign / band — is owned by `solver_convergence.rs` + IV-1, not this row | `SingleTetMesh` + `BoundaryConditions` walking-skeleton pattern (alternative to SDF path for hand-authored scenes) | `have` (`5bab2e62`) | — |

### Tier 2 — Constitutive + multi-element (Phase 1–2 surface)

> **Standardization (2026-07-14):** rows 5–6 (`neo-hookean-uniaxial` /
> `multi-element-stretch`) folded into one headless validator
> `example-stretch-stress-test` at `examples/sim-soft/stretch/stress-test/`
> (modules `neo_hookean` / `multi_element`), together with Tier 1's row 4
> (`single-tet-stretch` → module `single_tet`) — one uniaxial-stretch domain →
> one stress-test (the domain straddles Tier 1 / Tier 2). Row numbers preserved
> as immutable IDs; the standalone `neo-hookean-uniaxial` / `multi-element-stretch`
> crates no longer exist.

| # | Concept | Simplest example | Capability shown | Status | Gap to land |
|---|---|---|---|---|---|
| 5 | Neo-Hookean uniaxial stretch | `neo-hookean-uniaxial` — 12-point **traction-free** uniaxial stretch sweep on a single `Tet4` (`F = diag(λ, λ_t, λ_t)` with `λ_t` per stretch via 1-D Newton on the traction-free transcendental) driving the real `NeoHookean` into a force-stretch curve (JSON + `plot.py`); reads `ValidityDomain` declaration + per-point in-domain SV check. Demonstration gates (inner-Newton convergence, traction-free `P_22`/`P_33`, `P_11` monotonicity + sign, in-domain) — constitutive **closed-form correctness** is owned by the `NeoHookean` lib tests (Rule-B de-frag); the example emits the real observed curve (no analytic overlay — nothing unverified in the artifact) | Demonstrates the canonical traction-free NH stress-strain **curve** (real `first_piola` / `energy` observed) + `ValidityDomain` declaration semantics; constitutive **closed-form correctness** is owned by the `NeoHookean` lib tests, not this row. Solver-side panic enforcement of the boundary is **not** in this row's scope — that's the IV-7 follow-on (vertex-permutation det F < 0 test, flagged in `multi_material_validity.rs` docstring) | `have` (`bd727023`) | — |
| 6 | Multi-element FEM stretch | `multi-element-stretch` — N-tet `HandBuiltTetMesh` (e.g., 8-tet hex split) under uniform Dirichlet stretch; a heavy end-to-end FEM solve demonstrating per-tet `F` uniform `diag(λ,1,1)` across all 48 tets + bounded `P_11` uniformity spread + interior vertex reproduces `D·X_rest`. The assembly machinery is lib-owned (`uniaxial_fem_coupon.rs` = multi-element 384-tet **traction-free** patch test); this row's distinct axis is the **constrained-transverse** `diag(λ,1,1)` config + explicit all-48 readout. Real per-tet `P`/ψ emitted to JSON; closed-form correctness owned by the `NeoHookean` lib tests (Rule-B de-frag) | Phase 2 multi-element assembly; per-tet local → global stitch via `referenced_vertices` | `have` (`3a569258` + docs `4037db2d`) | — |

### Tier 3 — Multi-material spatial fields (Phase 4 surface)

> **Standardization (2026-07-14):** rows 8–9 (the `ScalarField` pair) folded
> into one headless validator `example-scalar-field-stress-test` at
> `examples/sim-soft/scalar-field/stress-test/` (modules `layered` / `blended`)
> — one domain → one stress-test. Row numbers preserved as immutable IDs; the
> standalone `layered-scalar-field` / `blended-scalar-field` crates no longer
> exist. Rows 10–11 (`bonded-bilayer-beam` / `concentric-lame-shells` — the
> FEM-solving bonded multi-material pair) likewise folded into one headless
> validator `example-bonded-stress-test` at
> `examples/sim-soft/bonded/stress-test/` (modules `bilayer_beam` /
> `lame_shells`) — solver-driven multi-material FEM validated against
> closed-form elasticity (Euler-Bernoulli composite beam / piecewise-Lamé
> shell). The standalone `bonded-bilayer-beam` / `concentric-lame-shells`
> crates no longer exist; the rows 10–11 entries below are retained as
> immutable capability IDs.

| # | Concept | Simplest example | Capability shown | Status | Gap to land |
|---|---|---|---|---|---|
| 8 | Layered scalar material field | `layered-scalar-field` — `LayeredScalarField` over a SDF stack (3 concentric shells); emit per-tet material-tag PLY (color = layer ID) | sharp-boundary spatial composition; `Field<f64>: Send + Sync` trait-object stack | `have` (`8fc28bad` + N+2 `233b687d` + N+3 `ce6485f4`) | — |
| 9 | Blended scalar material field | `blended-scalar-field` — `BlendedScalarField` smooth sigmoid transition between two layers; same-shape PLY as #8 but with per-tet (μ, λ) gradient instead of step | smooth-transition material grading (vs #8's sharp CSG step) | `have` (`d56b045e` + N+2 `ac19347f` + N+3 `6108c6a1`) | — |
| 10 | Bonded multi-material continuity | `bonded-bilayer-beam` — bonded soft+stiff bilayer cantilever beam under tip load; assert displacement asymmetry vs single-material baseline | shared-vertex multi-material coupling (no inter-layer slip) | `have` (`c47d273c` + N+2 `dfdb984f` + N+3 `6f84c9cf`) | — |
| 11 | Three-shell concentric hollow sphere (IV-5) | `concentric-lame-shells` — `SoftScene::layered_silicone_sphere` with internal pressure load; assert radial displacement profile against Lamé closed-form for 3-layer composite | the canonical IV-5 multi-material + SDF + hollow-body integration test, exposed as a user-facing demo | `have` (`9ce57e7e` + N+2 `0223b5ca` + N+3 `50ad4f33`) | — |

### Tier 4 — Penalty contact (Phase 5 surface)

| # | Concept | Simplest example | Capability shown | Status | Gap to land |
|---|---|---|---|---|---|
| 12 | Drop-and-rest on plane | `soft-drop-on-plane` — soft sphere falls under gravity, settles on `RigidPlane`; assert final COM height = R + tolerance | one-way penalty contact + rest-state convergence (drop-and-rest) | `internal-fixture` (`contact_drop_rest.rs`) | wrap as `examples/sim-soft/soft-drop-on-plane/`; emit final-frame deformed-mesh PLY; assert quiescence (max velocity < threshold) |
| 13 | Hertzian sphere ↔ plane | `hertz-sphere-plane` — soft sphere quasi-statically pressed against `RigidPlane`; assert contact-patch radius vs Hertz analytic | analytical contact-pressure validation (Hertzian) | `internal-fixture` (`hertz_sphere_plane.rs`) | wrap as `examples/sim-soft/hertz-sphere-plane/`; assert patch radius within Hertz tolerance; emit pressure-vs-radius JSON |
| 14 | Compressive block under plates | `compressive-block` — soft cube between two `RigidPlane`s, top plate descends quasi-statically; assert engineering-stress curve | `PenaltyRigidContact` under bilateral compression (compressive block) | `internal-fixture` (`penalty_compressive_block.rs`) | wrap as `examples/sim-soft/compressive-block/`; emit force-displacement JSON; assert NH-derived stiffness slope |

### Tier 5 — Bridges + extensions (silicone-device path; **mostly blocked**)

> **Standardization (2026-07-14):** row 19 (`silicone-material-table`) relocated
> into the standardization layout as validator `example-material-stress-test` at
> `examples/sim-soft/material/stress-test/` (module `material_table`) — a
> singleton `material` domain (the constitutive-table oracle is unrelated to the
> Tier-6 synthesis device, so they are NOT one domain). Row number preserved as
> an immutable capability ID; the standalone `silicone-material-table` crate no
> longer exists.
>
> **Standardization (2026-07-15):** rows 15–16 (`mesh-scan-as-solid` /
> `solid-to-sim-soft`, the SDF-bridge pair — both directions of the
> `cf_design::Sdf` trait) folded into one validator `example-sdf-bridge-stress-test`
> at `examples/sim-soft/sdf-bridge/stress-test/` (modules `mesh_scan` /
> `solid_to_sim`) — one domain → one stress-test, the example counterpart to the
> `sim/L0/soft/src/sdf_bridge/` library module. Row numbers preserved as
> immutable capability IDs; the standalone crates no longer exist.

| # | Concept | Simplest example | Capability shown | Status | Gap to land |
|---|---|---|---|---|---|
| 15 | mesh-sdf → cf-design bridge | `mesh-scan-as-solid` — programmatic 12-tri cube fixture saved to STL + reloaded via `mesh_io`; SDF dispatched via `&dyn cf_design::Sdf` (post-PR3 F2 impl on `SignedDistanceField`); 17³ bulk grid PLY with `signed_distance` + `inside_raycast` extras; `PseudoNormalSign` inside-set proven equal to the closed cube by per-point set-equality (`raycast_inside = 9³ = 729`, divergence `= 729 − 343 = 386` as closed-form identities) + F2-caveat-absent identity | scanned-geometry-as-design-primitive (the literal-SDF path per device memo) | `have` (PR3 row 15) | — |
| 16 | cf-design → sim-soft bridge | `solid-to-sim-soft` — compose `Solid::sphere(R_OUTER).subtract(Solid::sphere(R_CAVITY))`; mesh via `SdfMeshedTetMesh::from_sdf(&solid, ...)` taking `&dyn cf_design::Sdf`; bridge-equivalence anchor vs `DifferenceSdf<SphereSdf>` baseline + single-material Lamé pressurization at row-11 geometry; mesher-version-robust structural count invariants (absolute counts unpinned, like row 11) | one-line glue from cf-design design surface to sim-soft simulation surface | `have` (PR3 row 16) | — |
| ~~17~~ | ~~`RigidSdfPrimitive` (non-planar rigid indenter)~~ | DELETED (PR2 iter 1, 2026-05-06) — substance subsumed by trait unification on `Sdf`. Any `impl Sdf` (`SphereSdf`, `MeshSdf`, cf-design `Solid`) is now a valid rigid primitive directly via [`PenaltyRigidContact::new`](src/contact/penalty.rs); no wrapper newtype needed. Non-plane primitives are first exercised at PR3 row 20 (Tier 6 device, scan-derived `MeshSdf`). | — | — | — |
| 18 | Force / pressure readout at contact | `contact-force-readout` — same scene as #14, but extract per-contact-pair pressure + integrated normal force into a JSON trace | quantitative readout for relative-comparison sim + post-cast empirical calibration loop (Fork B) | `missing` | **BLOCKED** on `PenaltyRigidContact::contact_pairs() -> impl Iterator<Item = ContactPair>` accessor + a per-pair force readout helper. Phase 5 sets up `ContactPair` + `ContactGradient` but no public force aggregator |
| 19 | Material reference (silicone constants) | `silicone-material-table` — direct-eval consumer of F4's `silicone_table.rs` const module: iterates 7 `pub const SiliconeMaterial` entries (`{Ecoflex 00-10/20/30/50, Dragon Skin 10A/20A/30A}`), dispatches each via `SiliconeMaterial::to_neo_hookean()`, probes at `F = diag(2, 1, 1)` (simple uniaxial stretch at the σ_100 anchor); 7 verify groups (ν=0.40 invariant + rest-config zero + closed-form P_11/P_22/ψ at rel-tol 1e-12 + hardness ordering + 21 captured-bits self-pins); JSON-only artifact per `feedback_visual_pass_collapses_for_json_rows` | engineering-grade material lookup, not "guess your Lamé params from a search bar" | `have` (PR3 row 19) | — |

### Tier 6 — Synthesis (the engineering goal)

| # | Concept | Simplest example | Capability shown | Status | Gap to land |
|---|---|---|---|---|---|
| 20 | Layered silicone device E2E (cavity-fit) | `layered-silicone-device` — scan-derived rigid indenter (12-tri cube fixture per row 15 sanitization-clean precedent) → cf-design heterogeneous CSG (`Solid::sphere(R_OUTER).subtract(Solid::from_sdf(scan, bounds))` via PR3 F5) → sim-soft tet mesh via `SdfMeshedTetMesh::from_sdf` (F1+F3) → 3-shell `MaterialField` per #8 with F4 const entries (outer + inner = `ECOFLEX_00_30`, middle = `DRAGON_SKIN_10A` proxy for conductive-composite) → static fit pose with the same scan SDF as `PenaltyRigidContact` rigid indenter (first non-plane consumer post-PR2 trait unification) → fit-tightness force readout (axial component of rest-state preload) + per-active-pair JSON + boundary-surface PLY + slab-cut at z = 0 PLY via [`sim_soft::viz`] public API (post-F1.5 retrofit; `displacement_magnitude` + `material_id` + `psi_j_per_m3` per-vertex). Mesh refined to `CELL_SIZE = 0.01 m` (post-F1.5 retrofit visual review surfaced that the original 0.02 m approximated the spherical envelope as a low-poly polyhedron; refinement gives ~10 lattice cells across the sphere radius for legible boundary surface). | the anchoring engineering goal — relative-comparison sim of cavity tightness on a poured-silicone layered device | `have` (PR3 row 20) | — |
| 21 | Manufacturing-target geometry (1 mm static fit) | `scan-fit-3layer-sleeve` — 3-layer sleeve wrapping cuboid scan stand-in (40×30×80 mm); engineering-grade silicone stack via F4 (`ECOFLEX_00_20` / `DRAGON_SKIN_10A` / `DRAGON_SKIN_20A` for 18/51/113 kPa); 1 mm static-overlap sphere probe via `PenaltyRigidContact`. First sim-soft user-facing demo of post-solve per-tet `Ψ` extraction (`F_t = D_curr · D_rest⁻¹` reconstructed inline; per-layer Ψ̄ aggregates with strict-monotone `Ψ̄_inner > Ψ̄_middle > Ψ̄_outer`). Rule-B `validator`: pipeline-emergent structural + physics gates (positive tet volume, mesh-partition, solver convergence, per-shell material routing, strict Ψ̄ ordering, displacement bound) read from the real solve — captured-bit self-pins stripped (constitutive/mesher correctness lib-owned). JSON + z-slab PLY. | direct manufacturing-target geometry — relative-comparison sim of a 6/4/4 mm 3-layer poured-silicone sleeve at gentle 1 mm static-overlap intrusion | `have` (post-PR3 row 21, commits `79b0d102` + `e3aeb997` + `b6035fa0` on `dev` 2026-05-08) | — |
| 21.5 | A2 LU-fallback acceptance (capsule + capsule) | `scan-fit-3layer-sleeve-v15` — geometry-pivot fork of row 21. Capsule scan (`SCAN_RADIUS=0.015`, `SCAN_HALF_HEIGHT=0.025`) + capsule probe (`PROBE_RADIUS=0.012`, `PROBE_HALF_HEIGHT=0.030`) at 1 mm static-overlap penetration, y=0 longitudinal slab cut through contact zone. Same 3-layer sleeve materials + F4 silicone-table + `LayeredScalarField` + `PenaltyRigidContact` as v1. Pre-A2 this geometry empirically tripped faer Llt non-PD at Newton iter 2-3 for every depth (1/2/4 mm) — the row 21 v1.5 spec was PARKED 2026-05-08. Post-A2: 4 fallback engagements at iters 2-5 (vertex 13271 at iter 2 matches parked memo exactly), Newton converges in 92 iters to 9.05e-11 (within `tol=1e-10`). `force_total_z = -164.8 N`, `n_active_pairs = 151`. Captured-bits self-pin: 11 EXACT counts + 7 REF_BITS. Strain-energy ordering INVERTS vs v1 flat-face (apex contact concentrates load in middle/outer via wrap-radial transmission); `verify_strain_energy_ordering` retained under `#[allow(dead_code)]` per row 25 precedent. MAX_NEWTON_ITER bumped 50→100 for LU-fallback's slow-convergence tail. | A2 acceptance: vertex 13271 at iter 2 IS the parked memo's named trip; fallback engages, convergence completes, captured-bits self-pin | `have` (post-A2 arc, 2026-05-11) | 2 mm + 4 mm cases (4 mm hits Armijo stall at iter 18 — downstream Newton-basin limit; addressed by v2 quasi-static ramp, reduced probe radius, or non-cap probe per parked memo pattern (nn)) |
| 22 | Manufacturing-target deep-intrusion ramp (6 mm) | `scan-fit-3layer-sleeve-ramp` — same geometry + materials as row 21; 12-step quasi-static `replay_step` ramp at 0.5 mm/step from rest to 6 mm penetration. First sim-soft user-facing demo of `replay_step` chaining (`x_prev_k+1 = x_final_k`); circumvents the single-step Newton-basin collapse that limits row 21 v1 to 1 mm. Rule-B `validator`: pipeline-emergent structural + physics gates read from the real ramp (positive tet volume, per-step convergence, force-displacement monotone from step 2 onward, per-step Ψ̄ ordering at every intermediate equilibrium, displacement bound, per-shell material routing) — captured-bit self-pins stripped (constitutive/mesher correctness lib-owned). JSON `ramp_curve` array + post-F1.5 boundary-surface + slab-cut at z = 0 PLY emits via [`sim_soft::viz`] public API + `plot_ramp.py` matplotlib post-processing. **Wall: Neo-Hookean validity domain trips at `max_disp ≈ 7 mm`** (Phase 4 Decision Q fail-closed at `max_stretch_deviation < 1.0`); 8 mm user-target deferred to a wider-validity hyperelastic material-model arc. | deep-intrusion progression toward the user-target 8 mm physical depth — 6× v1's depth via multi-step Newton, full force-displacement curve as JSON | `have` (post-PR3 row 22 + v2.5 anchor cleanup, 2026-05-08) | Closed by row 23 (Yeoh, 2026-05-09) — Mooney-Rivlin was math-falsified on Smooth-On TDS data (Drucker-unstable C₀₁<0 on Ecoflex), pivoted to 2-param Yeoh per the Yeoh arc memo (`project_yeoh_hyperelastic_arc.md`) |
| 23 | 8 mm Yeoh consumer (wider-validity hyperelastic) | `scan-fit-3layer-sleeve-yeoh-ramp` — same scan + 3-layer sleeve + sphere probe geometry as rows 21 + 22; constitutive model swapped from Neo-Hookean to **Yeoh** (additive `C₂(I₁−3)²` extension over NH's deviatoric kernel, same NH-style compressibility); ramp extended to **16 × 0.5 mm = 8 mm**. First sim-soft user of the F4.0 generic-mesh-over-Material refactor (`SdfMeshedTetMesh<Yeoh>::from_sdf_yeoh` + `MaterialField::from_yeoh_fields_with_bounds` + `PenaltyRigidContactYeohSolver`). Material stack demonstrates BOTH constructor paths: inner = Path 2 via `from_effective_shore(DoubleZero(20.0))?.to_yeoh()` (parametric API; weight=1.0 at ECOFLEX_00_20 anchor, `ConstructionSource::Interpolated` provenance tag); middle/outer = Path 1 direct `DRAGON_SKIN_*.to_yeoh()`. **Headline: Yeoh reaches 8 mm cleanly in 77 Newton iters at step 16 (cap 150, 73-iter margin; residual 9.87e-11), force at 8 mm = 49.3 N vs row 22's 25.6 N at 6 mm** — Yeoh's `C₂(I₁−3)²` stiffening engages at the higher-strain regime where NH would have refused. ★ Robust 8 mm convergence requires routing the calibrated Yeoh validity bounds (`from_yeoh_fields_with_bounds`, `0.8·λ_break ≈ 5.8-8.8` tensile caps); the bounds-less `from_yeoh_fields` silently falls through to the legacy NH σ=2.0 ceiling and fail-closes near step 16 (fixed arc-close-b rung 2). Rule-B `validator`: structural + physics gates read from the real ramp (positive tet volume, per-step convergence, force-disp monotonicity, strict Ψ̄ ordering, disp bound, per-shell material routing) — captured-bit self-pins stripped (constitutive/mesher correctness lib-owned). JSON `ramp_curve` + post-F1.5 boundary-surface + slab-cut at z = 0 PLY emits via [`sim_soft::viz`] public API + `plot_ramp.py`. | user-target 8 mm physical intrusion via wider-validity hyperelastic; arc thesis "Yeoh fixes the row-22 NH wall" validated end-to-end | `have` (post-PR #235 Yeoh foundation, row 23 commits `3e1a5e53` + `f317432e` + `85f81315` + `6b7d1337` + `da1280fc` on `dev` 2026-05-09) | — |
| 24 | Axial-zoned 8 mm Yeoh (soft tip / stiff anchor) | `scan-fit-3layer-sleeve-yeoh-axial-zoned-ramp` — same geometry + ramp depth as row 23; ONLY differentiator is axial zoning of the material field. First sim-soft user of `BlendedScalarField` over an axial half-space SDF composed onto `LayeredScalarField` — 2-zone axial × 3-shell radial = 6 anchor cells with one-Shore-step contrast per family (Ecoflex 00-20 → 00-30 inner, DS10A → DS15 middle, DS20A → DS30A outer). Per-parameter independent composition (one `BlendedScalarField` each for μ, C₂, λ) sharing the same axial SDF + band → smoothstep weight bit-identical at every probe → band-zone Yeoh sample is a coherent `(μ_blend, c2_blend, λ_blend)` triple. **No new `Field<f64>` impl required**; only an inline 5-line `Sdf`-impl `AxialHalfSpace`. ★ Robust 8 mm convergence requires routing the calibrated Yeoh validity bounds (`from_yeoh_fields_with_bounds`, blended axially like μ/C₂/λ); the bounds-less `from_yeoh_fields` fail-closes on the legacy NH σ=2.0 ceiling near step 16 (fixed arc-close-b rung 4, same bug as row 23). Rule-B `validator`: pipeline-emergent structural + physics gates read from the real ramp (positive tet volume, fully-populated 9-cell zone × shell decomposition, per-step convergence, force-disp monotone, per-zone radial + per-shell axial `Ψ̄_proximal > Ψ̄_distal` ordering, disp bound, per-shell material routing) — captured-bit self-pins + the blend-zone smoothstep self-mirror stripped (constitutive/blend/mesher correctness lib-owned). JSON `ramp_curve` + 5-section schema (scalars + `axial_zoning` metadata + nested per-shell proximal_anchor + distal_anchor + ramp_curve + final_pairs). Post-F1.5 viz retrofit: emits boundary-surface PLY + slab-cut at x = 0 PLY via [`sim_soft::viz`] public API (replaces the pre-F1.5 x-slab centroid cloud emit). | composition primitive validation: `BlendedScalarField` × `LayeredScalarField` produces architecturally clean axial × radial material fields on the Yeoh foundation, no plumbing change needed for future rows | `have` (post row 23, 2026-05-10) | v4 (Cu-mesh sub-layer; gated on faer LU fallback for SPD pivot at finer cells); vN (real anatomy scan via `mesh_sdf::SignedDistanceField`); v5+ (3-param Yeoh post-cast Fork-B C₃ calibration) |
| 25 | Open-mouth insertion-cavity (F1.6 viz arc) | `scan-fit-3layer-sleeve-yeoh-axial-zoned-ramp-open-mouth` — open-mouth fork of row 24. Cavity used for boolean subtraction extended on +z by `MOUTH_EXTENSION_PLUS_Z = WRAP_THICKNESS + 0.001 m` so the cavity pokes through the outer envelope's +z face, leaving the wrap with an open mouth on +z (cup-with-open-top — the production-target geometry of an insertion-cavity device). Load case replaces row 24's spherical probe with a `Solid::cuboid().offset(1 mm)` plug at xy interference of 0.1 mm descending through the open mouth into the cavity walls. Ramp depth **8 × 0.5 mm = 4 mm** (vs row 24's 8 mm). Rule-B **`demo`** (arc-close b rung 5): its ramp-physics oracles are inherently inapplicable (interference-fit plug inverts force sign + strain ordering; first ~4 steps degenerate iter=0/no-deform until the plug engages) → no robust physics oracle → de-gated (all captured-bit/count/material/compile-time asserts stripped, incl. the `#![allow(dead_code)]` dead-gate block). Routes calibrated Yeoh bounds via `from_yeoh_fields_with_bounds` (same fix as r23/r24) so the demo reaches 4mm cleanly; deeper is a separate mesh/plug-corner limit. Constitutive/blend/mesher correctness lib-owned + sibling-validator-covered; payload = open-boundary viz. | F1.6 viz arc payload: validates that the F1.1 public viz primitives (`boundary_surface`, `slab_cut`) handle open-boundary topology — the boundary mesh now includes the cavity-wall surface joining the outer envelope at the rim, all in one connected mesh, no per-row plumbing. Production-target geometry for the layered silicone device's iter-1 cast. | `have` (post-F1.5, 2026-05-10) | F1.7+ row-25-specific load-case gates; deeper-than-4mm penetration via finer mesh near rim corners or aggressive plug chamfer; vN real-scan replacement via mesh_sdf bridge |

### Deferred to future arcs (Q3, Q4 decisions — out of this inventory)

- **Forward-map gradcheck demo** (former row 21) — `SkeletonForwardMap` / `Differentiable` / `NewtonStepVjp` autograd surface. **Deferred to a future "differentiable-sim" arc.** Reasoning: the autograd surface deserves its own arc with multiple coordinated examples (forward gradcheck, scene-param sweep, parameter-fitting demo), not one orphan row here. The thermo-RL-loop motivation is real but tangential to the layered silicone device anchoring goal of this arc.
- **Reward breakdown demo** (former row 22) — `RewardWeights` / `RewardBreakdown` readout. **Deferred indefinitely.** Reasoning: no real `VecEnv`-wrapping consumer exists for sim-soft today; authoring a demo over an extension point with no consumer is cargo-cult coverage. Re-evaluate if and when a sim-soft RL env materializes.

---

## Identified platform gaps so far

Sketched during the 2026-05-04 conversation; refine and re-validate during iteration:

- `mesh-sdf → cf-design` bridge — `MeshSdf` should be wrappable as a `cf_design::Solid` primitive composable with parametric ones. (Memory's pre-2026-05-04 line "`mesh-sdf` adapter stays unbuilt absent a real consumer" is now obsolete — silicone device scan workflow IS the consumer.)
- `cf-design → sim-soft` bridge — `cf_design::Solid` should impl `sim_soft::Sdf` (or an equivalent one-line conversion). Today every user wires their own glue.
- ~~`RigidSdfPrimitive`~~ — **CLOSED at PR2 iter 1 (2026-05-06, `71e9bb77`)** by `RigidPrimitive` → `Sdf` trait unification. Any `impl Sdf` is a valid rigid primitive directly via [`PenaltyRigidContact::new`](src/contact/penalty.rs); no wrapper newtype needed. See iter-6 entry below.
- Force / pressure readout at contact — needed for relative-comparison sim and empirical-calibration loop.
- Material reference — code constants module, builder, or doc table for `{Ecoflex 00-10/20/30/50, Dragon Skin 10A/20A/30A}` with Young's modulus / ν / source. Stops "look it up somewhere" friction.
- `sim/L0/soft/examples/` directory — does not exist today. Closest end-to-end demo is `SoftScene::layered_silicone_sphere` inside `src/readout/scene.rs` (a fixture in production source, not user-facing).
- Crate naming inconsistency — directory `sim/L0/soft/` vs crate name `sim-soft` (minor; flag if it adds friction during examples authoring).

Not in this list (deliberate Fork B parking):

- Phase H — F-bar reformulation / Tet10 quadratic elements / accurate ν=0.49 silicone behavior. Parked. Calibration absorbs the ν=0.40 locking error for now.
- Coulomb-Stribeck friction. Parked unless a specific example demands it.
- Differentiable-design adjoint methods. Parked.
- Multi-physics (electrical/thermal coupling for the conductive composite middle layer). Parked.

---

## Open scope questions — answers + remaining `?`

### Q1: Capability-coverage examples not on the silicone-device path?

**Recommendation: yes, bounded by "every public-API surface gets ≤ 1 canonical example."** Tiers 1-4 of the inventory (rows 1-14) cover the existing platform museum-tour. Tier 5 (rows 15-19) is the device-path bridge work. Tier 6 (row 20) is the device synthesis. Rows 21-22 are flagged `?` for explicit defer-or-include.

**Why this bound:** (a) the mesh-v1.0 arc shipped 9 capability-coverage examples (PR #224 + #225) — sim-soft has roughly the same exposed-API surface area, so ~10-14 capability-coverage rows is precedent-aligned; (b) per `feedback_examples_drive_gap_fixes`, examples *expose* gaps — Tier 5 only became visible because Tier 1-4 was authored first; (c) per `feedback_museum_plaque_readmes`, the museum-tour is itself the value, not a dilution of the device-driven priority.

**Concrete cap:** ~14 user-facing examples planned for ship, plus the ≤ 1 silicone-device synthesis row gated on bridge work, with rows 21/22 to be confirmed-or-dropped before any execution. Total: ~15-16 user-facing examples.

### Q2: Ordering — capability-coverage vs silicone-device-driven vs interleaved?

**Recommendation: museum-tour order in the inventory table; PR-staging order driven by the silicone-device dependency chain.** These are two separate questions and conflating them is what made §Q2 hard to answer in the seed.

**Why:** the table is a *reference* — readers want to find "the canonical example for SDF→tet" without scanning a device-priority order; museum-tour ordering serves them best. *PR sequencing* is a different artifact (§Q5 below) and IS device-priority-driven, because per `feedback_examples_drive_gap_fixes`, what we author first is what reveals the bridges to fix — the device path drives that sequencing even though the table presents capability tiers.

### Q3: Where do user-facing examples live?

**Recommendation: `examples/sim-soft/<name>/` at the repo root.**

**Why:** the mesh-v1.0 convention (`examples/mesh/<name>/`) is the established repo pattern with shipped READMEs, layout docs, and a per-example template (verified via `examples/mesh/README.md` lines 141-153). Adopting it verbatim — same directory shape, same `[package].name = "example-sim-soft-<name>"` naming, same README template — costs nothing and locks one more layer of consistency across the repo.

`sim/L0/soft/examples/` (per-crate, native Rust convention) loses the cross-domain discoverability that `examples/EXAMPLES.md` provides and would be the only crate doing it that way. Reject.

**Doc obligation when first example lands:** add a `## fundamentals/sim-soft/` section to `examples/EXAMPLES.md` mirroring the existing `## fundamentals/sim-cpu/` and `## fundamentals/mesh/` (the mesh examples actually live at `examples/mesh/`, not `examples/fundamentals/mesh/` — the EXAMPLES.md doc is one PR behind reality and that drift is a small followup, not a blocker). Add a top-level `examples/sim-soft/README.md` mirroring `examples/mesh/README.md` with the same per-example template.

### Q4: README minimum for sim-soft examples?

**Recommendation: adopt the mesh-v1.0 template verbatim, with sim-soft-specific anchor + visual conventions:**

- **Concept paragraph** — the one capability this demonstrates (one sentence, the platform contract).
- **What it does** — input scene (mesh-or-SDF) + transformation applied (load / pressure / contact / step) + output written (PLY + JSON + optional PNG).
- **Numerical anchors** — math-pass-first per `feedback_math_pass_first_handauthored`. For SDF / meshing examples: tet count, vertex band counts, boundary-band non-emptiness asserts (already the pattern in `SoftScene::layered_silicone_sphere` lines 238-249). For FEM / contact / multi-material examples: per-vertex displacement targets, energy bounds, force-displacement slope, Lamé / Hertz / NH closed-form comparisons. Clean `cargo run --release` exit-0 IS the correctness signal.
- **Visuals** — written 1:1 with what the user opens in f3d / MeshLab. For deformation: emit boundary-face PLY of the deformed configuration; for material tagging: per-tet color PLY; for force-displacement: JSON traces (Python plot is post-hoc, not in-example). **f3d winding callout** per `feedback_f3d_winding_callout` if any example emits inward-wound shells.
- **Run** — `cargo run -p example-sim-soft-<name> --release` per `feedback_release_mode_heavy_tests`.

**Math-pass-first specifically:** mesh examples encode coords + windings + bbox numerically. FEM analogs: encode displacement magnitudes, energy values, residual norms, contact-pair counts. Per the `concentric_lame_shells.rs` precedent — the radial-displacement vs Lamé closed-form is exactly this, and that test should *be* the example.

### Q5: One PR or multiple?

**Recommendation: 3 PRs gated by the device-dependency chain, with internal commit segmentation per `feedback_pr_size_ci_economics`.**

- **PR1 — Capability-coverage core (Tier 1-3 rows 1-6, 8-11).** **10 user-facing examples** wrapped from existing internal fixtures. No new platform machinery. Lands `examples/sim-soft/` directory + `examples/EXAMPLES.md` doc updates + per-example READMEs. Internal commit segmentation: ~one commit per example for the two-pass review cadence (`feedback_one_at_a_time_review`). PR size precedent: PR #224 shipped 8 mesh examples; this is comparable.
- **PR2 — Contact + readout extensions (Tier 4 rows 12-14 + row 18 `force/pressure readout`).** **4 user-facing examples**. Internal-fixture wraps for #12-14, new code for #18. Foundation: rigid-contact trait unification onto `Sdf` (delete `RigidPrimitive`, `impl Sdf for RigidPlane`, generic `PenaltyRigidContact::new` over any `Sdf + 'static`); enables PR3 row 20's scan-derived non-plane primitive without per-shape boilerplate. Plus `sim/L1/sim-bevy-soft/` sibling crate for deforming-tet boundary re-extraction (Bevy real-time rendering convention introduced here, see Visualization convention below). Smaller LOC than PR1, larger architectural surface.
- **PR3 — Bridge + synthesis (Tier 5 rows 15-16, 19 + Tier 6 row 20).** Architectural: introduces `cf_design::Sdf` trait per Q1+Q5 decisions, migrates `sim_soft::Sdf` to a re-export, lands `mesh_sdf::SignedDistanceField` adapter and `Solid` impl. Plus the silicone-material-table and the layered silicone device E2E example. Heaviest architecturally; the device example may itself be many commits.

**Why this shape over "one big PR":** CI is ~25 min per PR per `feedback_pr_size_ci_economics`; gating PR2/PR3 work on PR1's user-visible review cycle catches museum-plaque issues early instead of shipping 14 examples then re-reading them all under one diff. Per `feedback_one_at_a_time` + `feedback_one_at_a_time_review`, examples are reviewed individually before the next lands — that fits commit segmentation, not PR size.

**Pre-squash tag** per `feedback_pre_squash_tag` for **PR3** since it introduces the `cf_design::Sdf` trait and likely adds book entries for it; tag `feature/sim-soft-examples-arc-pr3-pre-squash` before its squash-merge. PR1 / PR2 likely don't touch any book chapters, so no tag needed.

---

## Visualization convention (PLY/JSON vs Bevy)

> **2026-05-04 SUPERSEDED — pending viewer arc.** PR1 commit 1's visual pass (sphere-sdf-eval via MeshLab) surfaced a gap the PLY-vs-Bevy split below didn't anticipate: **field-over-space content** (signed distance, material tags, stress) has no native viewer in the workspace — MeshLab requires a per-example two-filter chain to render the per-vertex scalar; f3d ignores the scalar entirely. The user separately flagged that f3d was already underwhelming for the mesh-v1.0 examples. The fix is a unified Bevy-CLI viewer (`cf-view <path>`-shaped) that auto-colormaps per-vertex scalars (per-face deferred — not exposed by mesh-io today; see VIEWER_DESIGN.md Q4 + stress-test E1) and replaces both f3d and MeshLab for static-artifact review. **Sim-soft examples arc PAUSED at PR1 commit 1** until the viewer arc lands. New planning doc: [`docs/VIEWER_DESIGN.md`](../../docs/VIEWER_DESIGN.md). The split below is preserved for audit-trail context but is not the operative convention going forward.
>
> **Operative convention going forward (locked at this layer; details in viewer arc):**
> - Static fields + meshes: emit PLY (existing pattern), visual review via the unified Bevy viewer one-liner `cf-view <path>`.
> - Dynamics (Tier 4 + Tier 6): per-example Bevy app, unchanged from below.
> - JSON-only force-stretch / displacement traces (rows 4, 5, 6): unchanged — matplotlib post-hoc, not in viewer scope.

**(Original iter-2 content below — superseded as noted; preserved for audit trail.)**

**Decision: split by tier — static-artifact (PLY/JSON, f3d/MeshLab viewing) for Tier 1-3 + Tier 5; Bevy real-time playback for Tier 4 + Tier 6.**

**Why split rather than pick one:**

- The mesh-v1.0 convention (PLY → f3d) is correct for **static math-pass-first artifacts**. Mesh examples have no dynamics — what you write is what you show. SDF eval, meshing, constitutive stress curves, multi-material assignment, bridges-and-readouts — these are all "did the math right?" examples. PLY + JSON anchors are sufficient and the math-pass-first asserts ARE the correctness signal.
- `feedback_visible_contacts` is a **hard rule**: all contact and collision must be visually rendered. Tier 4's drop-and-rest, Hertz, and compressive-block examples are dynamic contact phenomena. Static post-step PLY snapshots are a degraded experience for those — you lose the *settling* in drop-and-rest, the *contact patch growth* in Hertz, the *compressive descent* in the block. Bevy gives you the dynamics.
- Tier 6 (the silicone device E2E) is the synthesis showcase — it deserves the full visual treatment. Bevy renders the layered body deforming under the scan-derived rigid indenter, with material-tagged colors and contact-pressure readout overlay.
- Tier 5 bridges (rows 15-19) are **plumbing examples** — "this scan loads as a Solid", "this Solid meshes via sim-soft", "this readout returns a force." Static PLY/JSON for those mirrors the mesh-sdf/mesh-measure precedent. The silicone device is where the bridges combine into a Bevy-rendered whole.

**Per-row visualization assignment:**

| Rows | Visualization | Output artifacts | Convention reference |
|---|---|---|---|
| 1, 2, 3, 8, 9, 10, 11, 15, 16, 19 | **PLY + JSON** | boundary-face / per-tet-tagged PLY, force/pressure JSON | `examples/mesh/README.md` template |
| 4, 5, 6 | **JSON only** | force-stretch / displacement JSON traces (no spatial artifact) | math-pass-first per `feedback_math_pass_first_handauthored` |
| 12, 13, 14, 17, 18, 20 | **Bevy real-time** | live deforming-tet-mesh render + headless-mode asserts | `examples/fundamentals/sim-cpu/` precedent |

**Bevy authoring convention:**

- Mirror the `examples/fundamentals/sim-cpu/` shape: a Bevy app that runs headless-mode asserts pre-render (the math-pass-first numerical anchors) AND visual-mode playback in default mode.
- Per `feedback_one_at_a_time_review`'s two-pass cadence: numerical asserts are Claude's pass (cargo run exit-0); visual playback is the user's pass.
- Material-tagged colors for multi-material rows (the layered device's three shells get three distinct colors).
- Contact-pressure visualization for Tier 4: per-vertex pressure as a colormap on the soft-body surface, contact-force arrows where the rigid primitive bites (per `feedback_visible_contacts`).
- `feedback_f3d_winding_callout` is f3d-specific and does not apply to Bevy-rendered examples.

**Cost reality check:**

Bevy examples are ~3-4× heavier per file vs PLY-emit (~200-400 LOC each vs ~50-100). At 6 Bevy examples here, that's ~6 × 300 = ~1.8k LOC of Bevy-app code. CI cost is non-trivial but the mesh-cpu examples at 60+ already absorb this cost; sim-soft adding 6 is incremental. The visual payoff for contact dynamics + the device synthesis is worth the LOC.

**Open follow-up:** does sim-soft need its own `sim-bevy`-style adapter to render deforming tet meshes (boundary faces re-extracted per frame from `x_current`)? Likely yes — author that adapter as part of PR2's first contact example, then reuse across rows 12-14, 17-18, 20. Land it in `sim/L1/sim-bevy/` (or a new `sim-bevy-soft` sibling) rather than baking into each example.

---

### Q1-Q5 status — all five resolved this round (head-architect calls)

1. **Q1 — `mesh-sdf → cf-design` adapter naming**: define `cf_design::Sdf` trait owned by cf-design; `impl cf_design::Sdf for mesh_sdf::SignedDistanceField` lives in cf-design (orphan-safe). No `MeshSdf` newtype — use the real type name everywhere. **Locked, see row 15 gap column.**
2. **Q2 — Row 7 `InversionHandling`**: **fold into row 5**. The Neo-Hookean uniaxial example extends through compressive inversion to show clamp behavior — coherent pedagogy, no separate row. **Locked.**
3. **Q3 — Row 21 forward-map gradcheck**: **defer to a future differentiable-sim arc**. The autograd surface deserves a coordinated arc, not an orphan example. **Locked.**
4. **Q4 — Row 22 RewardBreakdown demo**: **defer indefinitely** — no real `VecEnv`-wrapping consumer for sim-soft today. Re-evaluate if one materializes. **Locked.**
5. **Q5 — `cf_design::Solid` impl-ing `sim_soft::Sdf`**: **`impl cf_design::Sdf for cf_design::Solid` directly** (Solid's existing inherent `evaluate(Point3) -> f64` becomes the trait `eval`; `grad` analytical-per-primitive or central-difference fallback). Sim-soft's `Sdf` trait migrated to a re-export of cf-design's. **Locked, see row 16 gap column.**

---

## Iteration log

_(append session-by-session; date-stamped; what changed and why)_

- **2026-05-04 (seed):** Initial structure + scope + identified gaps. No table rows yet — that's the first iteration's work.
- **2026-05-04 (iter 1 — table draft + scope answers):** Recon read-pass: sim-soft `lib.rs` exports, `examples/mesh/` layout + README template, `tests/` directory shape (35 internal-fixture files), `SoftScene::layered_silicone_sphere`, cf-design `Solid` location, mesh-sdf `SignedDistanceField`. Filled the inventory table with **22 rows** organized into 6 capability tiers (Tier 1 SDF primitives + meshing, Tier 2 constitutive + multi-element, Tier 3 multi-material, Tier 4 penalty contact, Tier 5 bridges + extensions, Tier 6 synthesis), plus 2 `?` rows (forward-map gradcheck, reward breakdown) flagged out-of-scope-pending-user. Answered all five open scope questions: (Q1) ~14 user-facing capability-coverage examples bounded by ≤ 1 per public-API surface, plus the device synthesis row + ~2 `?` rows; (Q2) museum-tour ordering in the table, device-dependency-chain ordering in PR sequencing — these are different artifacts; (Q3) `examples/sim-soft/<name>/` at repo root, mesh-v1.0 convention adopted verbatim; (Q4) mesh-v1.0 README template with sim-soft-specific anchor + visual conventions (math-pass-first applies, FEM analogs for displacement / energy / residual / force-displacement); (Q5) **3 PRs** — PR1 capability-coverage core (Tier 1-3, ~10-11 examples wrapped from internal fixtures, no new machinery), PR2 contact + readout (Tier 4 + rows 17 + 18, RigidSdfPrimitive + force/pressure readout extensions), PR3 bridge + synthesis (Tier 5 rows 15-16-19 + Tier 6 row 20, mesh-sdf → cf-design bridge + silicone-material-table + layered silicone device E2E). Inventory table is now **executable** as a 3-PR plan once the 5 remaining `?` items are resolved.

  **Still open (`?` for user):** (1) naming for `mesh-sdf → cf-design` adapter — `MeshSdf` newtype vs `Solid::from_external_sdf<S: Sdf>` generic vs raw `SignedDistanceField`; (2) keep / drop / fold row 7 `InversionHandling`; (3) include row 21 `forward-map gradcheck` in this arc or defer to differentiable-sim arc; (4) include row 22 `RewardBreakdown` demo or defer until a `VecEnv` consumer exists (my read: defer); (5) `cf_design::Solid` impl-ing `sim_soft::Sdf` directly vs explicit conversion fn — shapes how every later example is authored.

  **Refinements observed but not actioned:** (a) `examples/EXAMPLES.md` is one-PR-behind reality — the mesh-v1.0 examples actually live at `examples/mesh/` not `examples/fundamentals/mesh/`, and that section needs a doc update at first sim-soft-example land; small followup, not blocking. (b) `Phase 5.5` retirement per memory's resume-here is mechanically consistent with this inventory — `RigidSdfPrimitive` (row 17) + pressure readout (row 18) are the two named items folded into PR2 here, and the parked items (Coulomb-Stribeck friction, insertion sweep) do not appear at all.

  **Next iteration entry point:** answer the 5 remaining `?` items above with the user, then re-validate the table by walking each row and confirming the "simplest example" sentence is concrete enough to author from. Math-pass-first numerical anchors should be made explicit per row before any execution.

- **2026-05-04 (iter 2 — head-architect calls + Bevy convention):** User granted head-architect authority on the 5 `?` items per `feedback_master_architect_delegation`. Recon read-pass: `cf_design::Solid` already exposes `evaluate(Point3<f64>) -> f64` as inherent method, no `Sdf` trait exists in cf-design today, so adding one is a clean greenfield decision (not a migration of an existing trait). Decisions locked:

  **Q1 (adapter naming):** `cf_design::Sdf` trait owned by cf-design (`eval(Point3) -> f64` + `grad(Point3) -> Vec3`). `impl cf_design::Sdf for mesh_sdf::SignedDistanceField` lives in cf-design — orphan-rule-safe since the trait is local. No `MeshSdf` newtype, use real type name.

  **Q2 (row 7 InversionHandling):** folded into row 5. Neo-Hookean uniaxial example extends through the compressive-inversion branch to exercise `ValidityDomain` clamping. Row 7 deleted from table.

  **Q3 (row 21 forward-map gradcheck):** deferred to a future differentiable-sim arc. Autograd deserves coordinated multi-example treatment, not an orphan row.

  **Q4 (row 22 RewardBreakdown):** deferred indefinitely until a real sim-soft `VecEnv` consumer materializes.

  **Q5 (Solid → Sdf glue):** `impl cf_design::Sdf for cf_design::Solid` directly. The existing inherent `Solid::evaluate` becomes the trait `eval`; `grad` is analytical-per-primitive or central-difference fallback. **Sim-soft's `Sdf` trait migrated to a re-export of cf-design's.** Breaking change accepted per user's foundational-fix preference. `SphereSdf`/`DifferenceSdf` stay in sim-soft as deprecated test-only primitives until cf-design's `Solid::sphere()`/boolean ops cover their use sites.

  **Bevy rendering convention (new Q6):** split by tier — static PLY/JSON for Tier 1-3 + Tier 5 (10 rows), Bevy real-time for Tier 4 + Tier 6 (6 rows). `feedback_visible_contacts` is the hard requirement that lands Tier 4 in Bevy. Bevy adapter for deforming tet meshes (boundary-face re-extraction per frame) is a sub-task of PR2's first contact example, lands in `sim/L1/sim-bevy/` or a sibling crate, reused across rows 12-14, 17-18, 20. Bevy authoring mirrors `examples/fundamentals/sim-cpu/` shape: headless-mode asserts + visual-mode playback under one `cargo run`.

  **Cascading updates:** PR sequencing in Q5 updated to reflect the new row counts (PR1 = 10 examples, was 11; PR2 unchanged at 5; PR3 = 4, was 5). Row numbering preserved (rows 7, 21, 22 deleted but remaining rows kept their numbers — re-numbering would invalidate the iteration log's row references).

  **Inventory is now executable as a 3-PR plan with all architectural decisions locked.**

  **Next iteration entry point:** before any execution, walk each row's "simplest example" sentence and convert it into a concrete numerical-anchor list (the math-pass-first asserts) — that's the last planning round before PR1 authoring begins. Could be done in this same multi-session arc, or could be the first sub-step of PR1 itself depending on how concrete the user wants the plan vs how much they want to leave to in-PR judgment.

- **2026-05-04 (iter 3 — PR1 commit 1 shipped + arc PAUSED for viewer gap):** PR1 commit 1 shipped at `93b4d5a4` — sphere-sdf-eval (row 1) at `examples/sim-soft/sphere-sdf-eval/`: 7 verify_* anchor groups, 11³ = 1331-point grid, PLY emit with `extras["signed_distance"]`. All asserts green (exit-0); pre-commit clean (fmt + clippy with file-level allows mirroring mesh-sdf-distance-query); cargo doc clean under `-D warnings`. Per-example template now established for PR1 rows 2-11.

  **Visual pass surfaced a load-bearing gap.** MeshLab opened the PLY but required a 2-filter chain (`Per Vertex Quality Function` with `func q = signed_distance` → `Colorize by Vertex Quality`) to render the field. Per-example friction across 10 PR1 rows. User then flagged that f3d had also been underwhelming for the mesh-v1.0 examples — geometry-only viewer, no scalar colormap, no field semantics. The PLY-vs-Bevy split locked in iter 2 conflated two cases: mesh-v1.0's content is geometric (f3d works), sim-soft Tier 1's content is field-over-space (no native viewer in the workspace). The fix is engine-level, not example-level: a unified Bevy-CLI viewer (`cf-view <path>`-shaped) that auto-colormaps per-vertex / per-face scalars and replaces both f3d and MeshLab.

  **Sim-soft examples arc PAUSED at PR1 commit 1.** Per `feedback_fix_gaps_before_continuing` — when examples reveal engine gaps, fix the engine first. Next 9 PR1 rows would compound the friction; better to surface and fix now than 9 examples later.

  **Visualization convention superseded** at the section banner above; operative go-forward convention is locked at the banner level (`cf-view <path>` for static fields + meshes; per-example Bevy unchanged for dynamics; matplotlib unchanged for JSON-only force-stretch traces). Detail decisions deferred to the new arc.

  **New arc opened: unified-viewer architecture.** Seed planning doc at [`docs/VIEWER_DESIGN.md`](../../docs/VIEWER_DESIGN.md). Branch strategy: stays on `feature/sim-soft-examples-arc` for the seed-doc commit (prose only, low conflict); when viewer code starts, branch off main as `feature/cf-viewer-arc` so the viewer ships as its own PR. Sim-soft branch rebases after viewer merges; sphere-sdf-eval's README updates to point at `cf-view <path>` post-rebase. Mesh-v1.0 retrofit to the unified viewer is a separate followup, not gated on the viewer arc itself.

  **Next iteration entry point for the sim-soft arc:** wait for viewer arc to ship; then resume PR1 at row 2 (`hollow-shell-sdf`) using the new viewer. **Next iteration entry point for the viewer arc:** see `docs/VIEWER_DESIGN.md` resume-here block.

- **2026-05-05 (iter 4 — row 5 `neo-hookean-uniaxial` shipped + Q2 fold partially un-folded):** Row 5 shipped at sim-soft `dev` HEAD (post-row-4 `5bab2e62`). 12-point **traction-free** uniaxial stretch sweep on a single `Tet4`, direct-eval NH constitutive surface vs closed form. 9 anchor groups (validity declaration; inner Newton convergence; closed-form `P_11`/`P_22`/ψ agreement; rest-config `to_bits == 0`; monotonicity + sign; sweep in-domain; sweep bit-equal). 48 bit-pins (`P_11`, `P_22`, ψ, λ_t × 12) under the IV-1 two-tier contract — same rustc 1.95.0 capture as IV-1's `c3729d4a` reference. Optional `plot.py` (PEP 723 `uv run`) renders 4 panels (`P_11`, ψ, λ_t, J) to `out/force_stretch.png` — first sim-soft example with a post-hoc matplotlib visual aid; pattern is reusable for future JSON-curve rows (6 `multi-element-stretch`, 18 `contact-force-readout`).

  **Architectural calls** (head-architect-delegated per `feedback_master_architect_delegation`):

  1. **F-form: traction-free `F = diag(λ, λ_t, λ_t)`** over constrained-transverse `F = diag(λ, 1, 1)`. The constrained variant has a clean closed form (no inner Newton) but produces a curve nobody recognizes; the traction-free variant is the canonical NH benchmark in mechanics literature, and the inner Newton is just an inversion of a strictly monotone 1-D function (`f' = 2μ λ_t + 2Λ/λ_t > 0`), arcsin-shaped, converging in ≤ 6 iter. Pedagogically right wins over math-convenient.
  2. **Scope: constitutive-only (Q2 fold partially un-folded).** Iter 2's Q2 decision folded row 7 (`InversionHandling`) into row 5; execution-time recon revealed that the actual `RequireOrientation` enforcement happens in the **solver** (`check_validity_at_step_start`, `backward_euler.rs:471-523`), not in NH itself. NH's `first_piola` would `expect`-panic on a degenerate F or return NaN on inverted F, but the slot-name structured-panic is solver-side. Authoring catch_unwind demos that trigger the solver panic from inside this row would mean embedding novel test machinery (the IV-7 follow-on flagged in `multi_material_validity.rs` docstring as "a valid follow-on") inside an example whose subject is the constitutive surface. That's scope creep dressed as completeness; punted to a future test or a future row dedicated to validity-fail-closed semantics. **Effective Q2 re-decision: row 5 covers the constitutive surface + `ValidityDomain` declaration read + in-domain SV check; the IV-7 vertex-permutation follow-on remains in `tests/` territory.**
  3. **Sweep range: `λ ∈ [0.15, 1.95]`, 12 asymmetric points.** In-domain bracket computed (compressive boundary at `λ ≈ 0.118` set by `λ_t → 2`; tensile at `λ = 2` set by `λ` directly). Asymmetric density mirrors the bracket structure: 27% margin compressive (sensitive boundary — small Δλ → large Δλ_t), 2.5% tensile (stable). Compressive sub-trace `{0.15, 0.20, 0.30, 0.50}` covers the "approach to inversion boundary" semantics the original Q2 fold called for, without crossing it.
  4. **Bit-pin scope: 48 (4 quantities × 12 points).** Pinning λ_t alongside the constitutive outputs gates the inner-Newton convergence path itself: a convergence-criterion or initial-guess change producing a different λ_t (even within rel-tol slack on `P_11`) would surface as a bit drift. Per `feedback_no_allergy_to_bold_choices` — pulling back to "P_11 + ψ only" was minimum-diff-defaulting; pinned everything deterministic.
  5. **Inventory wording correction.** Iter-2's "show `InversionHandling` / `ValidityDomain` clamp behavior" was a misframe — there is no clamp branch (NH is `RequireOrientation`, panicking; the solver gate panics with structured slot names). Updated row 5's wording to "validity-domain declaration read + in-domain SV check" + corrected the `Capability shown` column to scope solver-side panic explicitly to IV-7 follow-on. Per `feedback_no_rationalizing_doc_drift` — when iter-2 wording goes off-spec, fix the spec, not paper over it.

  **Banked patterns from row 5** (cross-arc, future-row-relevant):

  - (a) **Inner-Newton-as-direct-eval-helper pattern** — if the constitutive evaluation requires inverting a known monotone scalar function (transcendental in the closed form), do it inline with a small Newton helper rather than punting to a "needs a solver" framing. ≤ 30 lines including convergence-failure abort. Future rows that hit similar inversions (e.g., Mooney-Rivlin parameter fitting, stress-driven stretch lookup) can mirror.
  - (b) **PEP 723 `uv run plot.py` post-hoc visualization pattern** — first sim-soft example to ship one. JSON-curve rows that benefit from a 2-D plot can adopt the same shape: PEP 723 inline metadata block at the top, reads `out/<name>.json`, writes `out/<name>.png` next to it, opens interactive window when `DISPLAY` is set. PEP 723 means no manual `uv venv` / `pip install` — first run installs deps, subsequent runs reuse cached env. Reusable for rows 6 `multi-element-stretch` and 18 `contact-force-readout`.
  - (c) **Two-phase bit-pin authoring with TEMP capture print** — write `verify_*` without bit-pins first, run, capture observed via a temporary `println!` block emitting Rust array literals (`{:#018x},  // λ = {}` per row), lift into source as `const SWEEP_*_BITS: [u64; N]` arrays, remove TEMP block, re-run to confirm green. Cleaner than authoring placeholder zeros and reading them out of assert-failure messages.
  - (d) **Capture-provenance comment block IV-1-protocol-verbatim** — when bit-pin reference values are captured for a new path, the comment block above the const arrays should mirror IV-1's: capture date + commit + rustc version + platform; IEEE-754 determinism rationale; two-tier contract claim (rustc minor versions + `(macOS arm64, Linux x86_64)`); failure-mode protocol three-step diagnose order ending in "NEVER re-bake." Pattern lifts from `invariant_iv_1_uniform_passthrough.rs:106-138`.

  **Status drift on rows 2-4 in this inventory** — those rows shipped on `dev` (rows 2 `1578ae50`, 3 `aec2d07a`, 4 `5bab2e62`) but their Status fields still read `internal-fixture`. Row 5's commit doesn't update those (out of scope for this row's commit). Followup: a documentation-cleanup commit before PR1 ship walks the table and lifts shipped rows to `have` with their `dev` commit hashes; alternatively absorb into PR1's squash-merge-to-main moment, since at that point the inventory itself may be deleted per `feedback_code_speaks` ("delete this file after the cleanup arc completes").

  **Next iteration entry point:** continue PR1 at row 6 (`multi-element-stretch`) — N-tet `HandBuiltTetMesh` under uniform Dirichlet stretch, asserting per-element stress agreement. Pattern from rows 4 and 5 carries forward: hand-authored scene + asserts + JSON; row 6 should be JSON-only per inventory Q4 (no spatial visualization), so the row-5 plot.py pattern applies if a per-element stress curve makes sense.

- **2026-05-05 (iter 5 — PR1 closure: rows 6 + 8 + 9 + 10 + 11 shipped on `dev`, Status drift fixed):** Six rows shipped post-iter-4: row 6 (`multi-element-stretch`, `3a569258` + docs `4037db2d`), row 8 (`layered-scalar-field`, `8fc28bad` + N+2 `233b687d` + N+3 `ce6485f4`), row 9 (`blended-scalar-field`, `d56b045e` + N+2 `ac19347f` + N+3 `6108c6a1`), row 10 (`bonded-bilayer-beam`, `c47d273c` + N+2 `dfdb984f` + N+3 `6f84c9cf`), row 11 (`concentric-lame-shells`, `9ce57e7e` + N+2 `0223b5ca` + N+3 `50ad4f33`). 3-pass cold-read cadence (commit + N+2 + N+3) confirmed across rows 8/9/10/11; banked cross-arc patterns at `project_sim_soft_row_{6,8,9,10,11}_patterns.md` in private session memory. **PR1 sim-soft examples arc COMPLETE** (10 examples; row 7 InversionHandling folded into row 5 per Q2; row 1 shipped earlier via PR #226 cf-viewer arc). PR #228 squashes 18 commits into one main commit.

  **Status field drift fixed in this iter** — iter-4's flagged followup ("a documentation-cleanup commit before PR1 ship walks the table and lifts shipped rows to `have` with their `dev` commit hashes") landed via the N+1 review pass on PR #228: rows 1, 2, 3, 4, 6, 8, 9, 10, 11 status fields lifted from `internal-fixture` to `have (<commit-hash>)`; "Gap to land" column collapsed to `—` for shipped rows. Row 5 status format updated from `have (shipped 2026-05-05)` to `have (`bd727023`)` for parity with other shipped rows.

  **Open architectural calls** for next session: (1) PR-shape decision settled at PR #228 (Option A — single PR, squash-merge with `feature/sim-soft-pr1-pre-squash` audit-trail tag); (2) next arc choice — PR2 Tier 4 contact + readout (blocked on `RigidSdfPrimitive` + `PenaltyRigidContact::contact_pairs()` accessor + force/pressure readout helper) vs C2 thermo-RL per gameplan north star.

  **Banked follow-up at row 11**: IV-5 `PRESSURE` constant docstring at `tests/concentric_lame_shells.rs:289-293` quotes `~4.6e-4 m` for cavity-wall analytic — actual is `3.342e-4 m` per LU solve (verified bit-exact against IV-5's runtime `eprintln!`). Sim-soft commit candidate; documented in row 11 README's Tet4 caveat subsection.

  **Next iteration entry point** (post-PR-#228-merge): inventory becomes deletion candidate per `feedback_code_speaks` ("delete completed specs; the code IS the documentation"), or keep as PR2/PR3 planning doc — decision deferred to first session of next arc.

- **2026-05-06 (iter 6 — PR2 kicked off; rigid-contact unified on `Sdf` + row 17 deleted):** Next arc decision = PR2 Tier 4 contact + readout (per memory's resume-here open call; alternative was C2 thermo-RL). Long-running `dev` branch reopened off main `5ff3518d` for the PR2 work; squash-merge as a single PR per PR1 precedent.

  **Foundation refactor (C1):** Rigid-contact heterogeneity glue migrated from the crate-private `RigidPrimitive` trait (`sim/L0/soft/src/contact/rigid.rs`) onto the existing public `Sdf` trait (`sim/L0/soft/src/sdf_bridge/sdf.rs`). The two were isomorphic up to naming (`signed_distance ≡ eval`, `outward_normal ≡ grad`); collapsing onto `Sdf` aligns the rigid-contact surface with the SDF-first design philosophy memory ("SDF → cf-design::Solid → sim-soft::Sdf → BCC stuffing → FEM"). Concrete edits: delete `RigidPrimitive` trait + impl; `impl Sdf for RigidPlane` (delegates to inherent `signed_distance`/`outward_normal` methods, which stay for plane-specific call-site readability); `PenaltyRigidContact::primitives: Vec<Box<dyn Sdf>>`; constructors generic over `IntoIterator<Item: Sdf + 'static>` so existing `vec![plane]` callers pass through unchanged AND mixed-primitive `Vec<Box<dyn Sdf>>` callers compose via the new blanket `impl<T: Sdf + ?Sized> Sdf for Box<T>`.

  **Row 17 (`RigidSdfPrimitive`) DELETED** — substance subsumed by the trait unification; no per-shape wrapper newtype needed. PR2 row count drops from 5 → 4 (rows 12, 13, 14, 18). Row 20 (Tier 6 device) reference updated: scan-derived `MeshSdf` is the first non-plane `PenaltyRigidContact` consumer at PR3 ship time. Architectural note: PR2 examples are all plane-only on the consumer side; the trait unification is the *enabling* change for PR3, not exercised by a non-plane scene within PR2.

  **Q1 (sim-bevy-soft location)** locked: new sibling crate at `sim/L1/sim-bevy-soft/` (vs folding into `sim/L1/sim-bevy/`). Reason: `sim-bevy` is pre-soft, built around `sim-core` rigid-body + `cf-geometry`; folding would drag `sim-soft` into its dep set. The L1-sibling-per-L0-domain pattern (sim-bevy ↔ sim-core, sim-bevy-soft ↔ sim-soft) is cleaner.

  **PR2 commit plan**: 2 foundation commits (C1 trait unification + inventory edits, C2 sim-bevy-soft skeleton) + 4 rows × 3-pass cadence (~12 commits) = ~14 commits total. Slightly over `feedback_pr_size_ci_economics`'s 12-14 norm; tolerable since PR2 has more architectural surface than PR1.

- **2026-05-06 (iter 7 — PR2 deep-architecture session; C2 split into C2a/C2b/C2c/C2d, `Mesh::boundary_faces` + `cf-bevy-common` factor-out + trajectory-replay all locked):** User requested ceiling-level architectural pass on C2 per `feedback_planning_as_therapy`; granted head-architect authority per `feedback_master_architect_delegation` with explicit patience signal ("don't care much about amount of PRs … take time to do this correctly"). Outcome: three architectural decisions locked, then Q4 sim-bevy inclusion in cf-bevy-common corrected mid-iter via user question (initial "sim-bevy stays out" call was based on a speculative claim about file content that one Read tool call disproved). C2 split into four commits. C2a + C2a-N+1 shipped within this session.

  **Q3 (`boundary_faces` placement + idiom) locked:** trait method `fn boundary_faces(&self) -> &[[VertexId; 3]]` on `sim_soft::Mesh`, populated at construction by a `pub(crate)` free helper `boundary_faces_from_topology` (NOT a free function in sim-bevy-soft, NOT a default impl on the trait). Reason: mirrors the existing `materials() -> &[NeoHookean]` + `interface_flags() -> &[bool]` precedent precisely — cached `Vec<T>` field on each impl, populated by sister helpers `materials_from_field` / `interface_flags_from_field`. Putting boundary-face extraction in sim-soft (L0) means downstream consumers (sim-bevy-soft viz, future headless deformed-mesh PLY emit at row 12 gap, Tier 6 device asserts at row 20) don't need to depend on Bevy. To deviate from the trait/sister-helper precedent would be inconsistent with the closest existing convention.

  **Q4 (`cf-bevy-common` factor-out) locked (sim-bevy inclusion corrected mid-iter post-user-question):** YES, in PR2 as commits **C2b (cf-viewer migration) + C2d (sim-bevy migration)** — NOT deferred to a follow-up PR (supersedes the cf-viewer arc memo's "second consumer" hedge at `project_cf_viewer_arc.md` iter-2 still-open #1). **All three Bevy consumers migrate** to cf-bevy-common: cf-viewer (shipped at PR #226), sim-bevy-soft (born consuming it), and sim-bevy (currently has its own duplicate). User-corrected my earlier "sim-bevy stays out — sim-core-coupled, ~30% overlap" claim, which was based on speculation about what `sim/L1/bevy/src/camera.rs` *probably* contained given the crate's deps, not a read of the actual file. Recon (post-correction): zero sim-core references in the file (no `BodyId`, no `Model`, no `ModelDataPlugin`); ~95% identical to cf-viewer's `OrbitCamera` (11 fields + `position`/`orbit`/`zoom`/`apply_to_transform` identical); differences are mechanical reconciliation (cf-viewer's broader distance bounds + slower pan_speed default; cf-viewer's `normalize_or_zero` guards in `pan()`; cf-viewer's `framing_for_aabb` helper). Anti-pattern banked at `feedback_spike_before_trust_analytical` "Architectural claims about code structure" bullet. New crate at workspace root `cf-bevy-common/` (matches cf-viewer's location precedent), no `[package.metadata.cortenforge]` block (matches cf-viewer + xtask), `publish = false`. C2b migrates cf-viewer atomically with crate birth (no transient duplication). C2d migrates sim-bevy after C2c lands; resolves the three reconciliation points (defaults → cf-viewer's broader bounds win, sim-bevy's `spawn_orbit_camera` already overrides via `with_*` builders so its current view stays unchanged; pan guards → cf-viewer's safer version becomes the shared default; framing_for_aabb → shared, available to sim-bevy on opt-in). Layer-integrity exemption likely needed (mirrors cf-viewer's `d2eb7e89`); surface at C2b author-time.

  **Q5 (visual mode shape) locked:** trajectory-replay (β) — solver runs headless, captures `Trajectory { frames: Vec<Vec<f64>>, dt: f64 }`, Bevy app replays via `step_replay` system advancing a frame counter. NOT live-solver-inside-Bevy. Reason: user confirmed "live feeling" not needed yet; (β) decouples solver step time from rendering frame time, deterministic across CI headless and user windowed runs, and matches the inventory's "headless-mode asserts pre-render AND visual-mode playback in default mode" Bevy-authoring convention. Per-example `main.rs` runs the headless harness unconditionally (asserts always run, JSON always emitted), then conditionally spins up Bevy `App` for replay via env var / CLI flag.

  **C2 split into four commits (replaces single C2 in iter-6 plan):**
  - **C2a — `sim_soft::Mesh::boundary_faces` trait method.** SHIPPED `b7758457` (+ N+1 cold-read fixups `0dfff492`). New `fn boundary_faces(&self) -> &[[VertexId; 3]]` on the trait; new `pub(crate) fn boundary_faces_from_topology(tets: &[[VertexId; 4]]) -> Vec<[VertexId; 3]>` helper at `sim/L0/soft/src/mesh/mod.rs`; `boundary_faces: Vec<[VertexId; 3]>` cache field added to `SingleTetMesh` / `HandBuiltTetMesh` / `SdfMeshedTetMesh`; all three constructors wired. Algorithm: 4·n_tets oriented faces emitted using the right-handed-tet outward winding convention (opposite v0 → (v1, v2, v3); opposite v1 → (v0, v3, v2); opposite v2 → (v0, v1, v3); opposite v3 → (v0, v2, v1)) — `HashMap` canonical-key counts; faces hit once are boundary, faces hit twice are interior pairs that cancel. Output order deterministic via tet-id-then-face-id input walk (sister of `materials_from_field`'s I-5 determinism carry-forward). Tests (3 added at `mesh/hand_built.rs::tests`): `single_tet_boundary_faces_exact_pin` (4 faces, exact tuple match against the right-handed convention), `two_tet_shared_face_culls_interior_face` (6 faces, `{1,2,3}` canonical key absent from output), `uniform_block_2_cube_boundary_count_and_outward_winding` (48 faces; per-face right-hand-rule normal verified outward against cube center). All 235 sim-soft tests green; clippy + rustdoc clean. Pure additive; no API breakage.
  - **C2b — `cf-bevy-common/` workspace crate + cf-viewer atomic migration.** NEXT. New crate at workspace root `cf-bevy-common/`, modules: `axis.rs` (`UpAxis` + `flips_winding` + `vertex_position` swap helper), `camera.rs` (`OrbitCamera` + `framing_for_aabb` + `apply_to_transform` + `orbit_camera_input` + `update_orbit_camera`). Bevy 0.18 feature set mirrors cf-viewer. cf-viewer migrates inline in same commit (no transient duplication); existing 56/56 cf-viewer tests stay green. xtask layer-integrity exemption likely needed.
  - **C2c — `sim/L1/sim-bevy-soft/` skeleton crate consuming both.** AFTER C2b. tier=L1; modules: `mesh.rs` (`build_soft_mesh` + `update_soft_mesh_positions` from `x_current` flat slice), `trajectory.rs` (`Trajectory` resource + `step_replay` system), `plugin.rs` (`SoftBodyVisualPlugin` + `SoftMeshTopology` component). NO contact-pair gizmo primitives in C2 (defer to row 12); NO live-solver Bevy system (trajectory-replay (β)).
  - **C2d — sim-bevy migrates to cf-bevy-common.** AFTER C2c. New post-correction commit per Q4 above. Removes `sim/L1/bevy/src/camera.rs` (~245 LOC); `sim/L1/bevy/src/lib.rs` re-exports `cf_bevy_common::OrbitCamera` + systems via the existing `prelude`; `sim/L1/bevy/src/plugin.rs::OrbitCameraPlugin` either re-points to `cf_bevy_common`'s systems or is deleted in favor of the shared crate's plugin (decide at author-time). `sim-bevy/Cargo.toml` adds `cf-bevy-common` dep. Existing sim-bevy tests stay green (defaults reconciled to cf-viewer's broader bounds; sim-bevy's `spawn_orbit_camera` overrides via `with_*` builders so the actual spawned view is unchanged).

  **PR2 commit plan revised (iter-7, post-Q4-correction):** ~19 commits = C1 + C1-N+1 + C2a + C2a-N+1 + C2b + C2c + C2d + 4 rows × 3-pass = 7 + 12 = 19. Above 12-14 norm; user signed off on patience. Pre-squash tag `feature/sim-soft-pr2-pre-squash` recommended given cf-bevy-common births a new shared crate consumed by three Bevy consumers.

  **Banked at this iter (cross-arc-relevant patterns):**
  - **(a) cached-trait-query idiom** — when a derived per-mesh quantity needs zero-copy access from multiple consumers, mirror `materials() -> &[T]` + `_from_field` helper precedent (cached `Vec<T>` field, populated at construction by `pub(crate)` free helper). Reusable for any future per-mesh derived cache (e.g., per-vertex valence, per-edge length, per-face area).
  - **(b) "factor-when-second-consumer-arrives" hedge resolution pattern** — when an arc memo banks "factor only when second consumer emerges" and that second consumer materializes, two questions follow: (i) for each existing consumer beyond the second, **read its code** (not its crate-level deps) to assess overlap with the to-be-shared abstraction; (ii) do we factor in-arc or in a follow-up PR? (user patience signal removed the scope-creep objection; in-arc is correct because doing it now means the second consumer is born clean instead of needing a future migration). For sim-soft PR2: cf-viewer + sim-bevy-soft + sim-bevy all share ~95% of `OrbitCamera` surface; all three migrate (C2b cf-viewer, C2c sim-bevy-soft, C2d sim-bevy).
  - **(c) trajectory-replay vs live-solver-in-Bevy** — for soft-body Bevy viz under non-real-time use cases, trajectory-replay (β) decouples solver step time from rendering frame time and is deterministic across CI headless + user windowed runs. Live-solver shape becomes worth it only when interactive parameter sweeping is in scope (not for PR2-PR3).
  - **(d) read-the-file before architectural claims** — when assessing whether a third consumer aligns with a to-be-shared abstraction, **always Read its file** before making the alignment claim. Crate-level transitive dep graph is NOT a proxy for per-file content. Banked example: claimed sim-bevy's `OrbitCamera` was "sim-core-coupled (BodyId tracking, ModelDataPlugin scene-tree integration), ~30% overlap with cf-viewer's" without reading `sim/L1/bevy/src/camera.rs`. User caught with a single pointed question. Recon revealed: zero sim-core references in the file, ~95% identical to cf-viewer's. Cost of the recon would have been one Read tool call. Sister of `feedback_spike_before_trust_analytical` extended bullet "Architectural claims about code structure" — same anti-pattern at the architectural-recommendation abstraction level.

  **Next iteration entry point:** C2b — author `cf-bevy-common/` + migrate cf-viewer atomically. Recon needed: (i) cf-viewer's `src/camera.rs` (full file, candidate to move) + `src/lib.rs` UpAxis enum (lines 64-83) + `src/cli.rs` `--up=` flag wiring (already partially read 2026-05-06); (ii) xtask Layer Integrity exemption pattern at `d2eb7e89`; (iii) any `examples/*` callsites that import from cf-viewer (none expected, but verify before migration commit). After C2b ships: C2c (sim-bevy-soft skeleton), then C2d (sim-bevy migration — read `sim/L1/bevy/src/{camera,plugin,lib}.rs` for migration surface + verify sim-bevy examples don't import OrbitCamera by absolute path).

- **2026-05-06 (iter 8 — C2b shipped: `cf-bevy-common/` factor-out + cf-viewer atomic migration):** C2b shipped at `7905c07c`; N+1 cold-read fixups at `97ab41a2`. Pre-author deep-architecture session covered six sub-decisions; three flipped from default after user-prompted second-pass thinking ("lets talk about each of the 3"):

  **Locked design calls (C2b in-arc):**

  1. **Bevy feature set on cf-bevy-common = minimal** (`bevy = { workspace = true }` with no extra features). Workspace dep at `Cargo.toml:478` already declares `default-features = false, features = ["x11"]`; `{ workspace = true }` inherits that and nothing more. cf-bevy-common's code references only ECS + math + transform + input + app types, all in `bevy_internal`'s base (not feature-gated). Standalone `cargo build -p cf-bevy-common` succeeded on first try. Iteration budget for "cargo error → add feature" was unused. Cf-viewer's broader 13-feature set (PBR / winit / tonemap_luts / etc.) is for the rendering pipeline; cf-bevy-common doesn't pay that tax for code it doesn't run, and Cargo's additive feature unification means consumers don't pay extra either.
  2. **`OrbitCameraPlugin` bakes `update_orbit_camera.after(orbit_camera_input)` ordering in.** Resource-conflict serialization (input mutates `&mut Query<&mut OrbitCamera>`, update reads `&OrbitCamera`) aligns with tuple order today; sim-bevy's existing plugin omits `.after()` and works coincidentally via this serialization. cf-viewer's existing manual wiring used `.after()` explicitly; bringing it into the plugin makes the contract legible at the schedule level rather than implicit at the type level. Survives future scheduler tweaks or a hypothetical event-channel refactor that decouples the systems' resource borrows. Systems still exposed `pub` for consumers wanting to interleave their own systems between input and update.
  3. **`vertex_position` renamed to method form `UpAxis::to_bevy_point` + sister `UpAxis::to_bevy_normal`.** Was free function `vertex_position(&p, up)` and private `vertex_normal(n, up)` colocated with `build_face_mesh` in cf-viewer/src/mesh.rs. The function is mis-named for its actual scope — called from 6 sites across mesh conversion, light placement, sphere positioning, AABB framing; it's the up-axis convention's projection function, not a mesh-conversion helper. C2b is the cheapest naming moment in the function's lifetime (after C2c + C2d, +N callsites would propagate the old name). Receiver-style `up.to_bevy_point(&p)` drops the `*up` deref at every Res<UpAxis> callsite. The "polish later" default I initially proposed had no natural trigger; user opened all 3 for discussion (`feedback_genuine_agreement`) and I flipped this one. The other two locks (1) + (2) held with sharper rationale.

  **Atomic migration shape:** new `cf-bevy-common/` workspace crate + `cf-viewer` migrated in single commit, no transient duplication. Files: `cf-bevy-common/{Cargo.toml, src/{lib,axis,camera}.rs}` born; `cf-viewer/src/camera.rs` deleted (333 LOC moved + adapted); `cf-viewer/src/{lib,mesh,main}.rs` import paths updated; `cf-viewer/src/cli.rs` untouched (uses `crate::UpAxis` which resolves via `pub use cf_bevy_common::axis::UpAxis;` re-export in lib.rs); workspace `Cargo.toml` adds member + `[workspace.dependencies]` entry; `xtask/src/grade.rs` Layer Integrity exemption extended (`cf-viewer | cf-bevy-common`) mirroring `d2eb7e89` pattern; `.github/workflows/quality-gate.yml` `tests-debug` list adds `-p cf-bevy-common`. 12 files, +326 / -181.

  **Test count:** 56 cf-viewer pre-migration → 42 cf-viewer + 15 cf-bevy-common = 57 post-migration (+1 new `to_bevy_normal_matches_point_swap` test added; sister tests for points migrated verbatim).

  **Pre-commit fmt-`--all` gotcha 5-for-5** — 5th consecutive Rust-touching commit (3 + 4 + 5 + 6 + C2b) bit by per-crate-clean-but-`--all`-flagged drift. C2b's drift: a 2-line tuple-arg reflow in `OrbitCameraPlugin::build`. Per-crate `cargo fmt -p cf-bevy-common -- --check` clean; `cargo fmt --all -- --check` flagged it. Pre-commit hook caught the pre-stage state once `--all` ran. **Calibrated rule stands: always run `cargo fmt --all`, never trust per-crate.** Banked at `project_git_and_hook_gotchas` for canonical record.

  **N+1 cold-read findings (5 doc-rot fixups at `97ab41a2`):** all in the new pattern variant **"code-moves-to-shared-crate doc-rot"** — when a function/component moves from a single-consumer crate to a shared crate, sweep the new shared crate's docstrings + test names for predecessor-crate-history references (specific commit numbers, predecessor-specific naming) that are opaque to fresh consumers. Specific findings:
  1. `cf-viewer/README.md`: test count "56 unit tests... orbit-camera framing" → "42... orbit camera lives in cf-bevy-common (15 tests)".
  2. `cf-bevy-common/src/axis.rs`: "reflection convention from cf-viewer's commit 3 + commit 6" → "single-axis swap parameterized by --up=<...>".
  3. `cf-bevy-common/src/camera.rs::framing_for_aabb` opens with "Frame the input AABB the same way the cf-viewer commit-3 placeholder did" → "Frame the input AABB corner-on".
  4. `cf-bevy-common/src/camera.rs` test docstring "reproduces the cf-viewer commit-3 corner-on view" → "produces the corner-on view".
  5. `cf-bevy-common/src/camera.rs` test fn `framing_for_aabb_matches_static_placeholder_corner_on` → `framing_for_aabb_corner_on_at_one_and_a_half_diagonal` (behavioral name).

  Sister of cf-viewer arc iter 1.10's "doc-coherence-after-tool-migration" pattern (sphere-sdf-eval README "for external colormap rendering" → "for colormap rendering" post-cf-view in-tree migration). Same shape at a different abstraction layer: that one was about ADJACENT prose drifting in meaning when the workspace gained a new tool; this one is about the EXTRACTED code's docstrings retaining its predecessor's history when shared with new consumers.

  **Banked at this iter (cross-arc-relevant patterns):**
  - **(a) discussion-then-decide for sub-architectural calls** — when proposing 3 default-status-quo decisions to the user, the user replied "lets talk about each of the 3" rather than approving en bloc. Treating this as a genuine request for re-thinking (not deference performance) per `feedback_genuine_agreement` flipped one of three — the flipped call was the bolder one (rename now vs polish later), proven correct because the migration was already touching every callsite. The two that held got sharper rationale (resource-conflict scheduler trace for the `.after()` decision; verification that workspace-default Bevy features were already minimal). Pattern: a user "lets talk" signal is permission to reach further architecturally, not just a request to re-state defaults.
  - **(b) PR2 commit count creep banked accurately** — original iter-6 plan was ~14 commits (single C2). Iter-7 deep-arch session split C2 into four (C2a/b/c/d) → ~19. C2a + C2b each shipped with N+1 cold-read fixups → ~20. Iter-8 N+1 catches doc-rot in ~5 places per commit when the commit births new shared abstractions. Future architectural-shape commits should plan +1 cold-read commit each, not just code-shape commits, and the count should reflect that as a baseline.

  **Next iteration entry point:** C2c — author `sim/L1/sim-bevy-soft/` skeleton consuming cf-bevy-common (UpAxis + OrbitCameraPlugin) + sim-soft (Mesh trait + `boundary_faces` method). tier=L1; modules: `mesh.rs` (`build_soft_mesh` + `update_soft_mesh_positions` from `x_current` flat slice), `trajectory.rs` (`Trajectory` resource + `step_replay` system advancing a frame counter), `plugin.rs` (`SoftBodyVisualPlugin` + `SoftMeshTopology` component). Per Q5 lock: trajectory-replay (β), NOT live-solver-inside-Bevy. NO contact-pair gizmo primitives in C2c (defer to row 12).

- **2026-05-06 (iter 9 — C2c shipped: `sim/L1/sim-bevy-soft/` skeleton crate):** Pre-author deep-architecture session covered eight sub-decisions; user requested a re-think on three (Q3 trajectory carry, Q6 bevy features, Q8 normals) per `feedback_genuine_agreement` "lets talk" pattern banked iter-8 (a). All three flipped from default after second-pass thinking — the deeper-architecture answer beat the minimum-diff status quo in each case.

  **Locked design calls (C2c in-arc), three flipped + five held:**

  1. **Trajectory storage = `Component` with `#[require(Mesh3d)]`** (flipped from initial Resource recommendation). Re-recon revealed PR2 + PR3 known-row scope is entirely single-soft-body (rows 12, 13, 14, 18, 20 — multi-rigid-primitive scenes treat planes as static gizmos, NOT as Trajectory consumers). With migration risk effectively zero, the call shifts to idiom + lifetime semantics: Bevy 0.18's `#[require(Mesh3d)]` annotation expresses the dependency at the type level ("Trajectory is meaningless without a Mesh3d"), no separate marker Component needed (`&Trajectory` itself identifies the soft-body entity in queries), despawn cascades free trajectory data automatically, tests spawn-then-assert is cleaner than world-construct-then-assert. Flipped from minimum-diff Resource per `feedback_no_allergy_to_bold_choices` + `feedback_foundational_ordering`.

  2. **Bevy feature set = minimal `["bevy_render", "bevy_asset"]` at the crate level; consumers bring rendering pipeline via Cargo additivity** (flipped from "mirror sim-bevy verbatim"). Right model is **cf-bevy-common's precedent extended one level up**, NOT sim-bevy's verbatim. cf-bevy-common's Cargo.toml comment frames it: "code uses only ECS + math + transform + input + app surface… Downstream consumers bring their own rendering-pipeline features via Cargo's additive feature unification." Same shape applies to sim-bevy-soft. Library-level type references are: `Plugin`, `App`, `Update`, `Time<Real>`, `Query`, `Res`/`ResMut`, `Component`, `Resource`, `Vec3`, `Transform` (all base) plus `Mesh` + `Mesh3d` + `ATTRIBUTE_POSITION`/`NORMAL` + `Assets<Mesh>` (covered by `bevy_render` + `bevy_asset`). NOT `StandardMaterial` (consumer wires the material), NOT `Camera3d` (consumer spawns the camera). The "mirror sim-bevy" call conflates "support library" with "workbench" — sim-bevy IS the workbench (spawns cameras, lights, gizmos, registers materials); sim-bevy-soft is one level lower (geometry + topology + replay). Each example brings its own full feature set; PR3 row 20 (mixes sim-bevy + sim-bevy-soft) gets sim-bevy's set additively. Flipped per `feedback_no_allergy_to_bold_choices`.

  3. **Per-frame `Mesh::compute_smooth_normals()` after each position write** (flipped from initial "once at load + double_sided"). Bevy 0.18's `bevy_mesh::Mesh::compute_smooth_normals()` (verified at `bevy_mesh-0.18.1/src/mesh.rs:1230` — area-and-corner-angle-weighted, panics if missing positions/indices/triangle-list, those preconditions all met by `build_soft_mesh`) is a one-method call with ~10K float ops per frame at PR2 mesh sizes (hundreds of vertices). At 60 Hz: <0.1 ms CPU, negligible GPU bandwidth (one extra `ATTRIBUTE_NORMAL` write per frame). The "once at load + double_sided" call had visual lag at row 14's ~30-50% squish (lit/dark transitions stuck where rest sphere had them); per-frame recompute removes that artifact entirely. `cull_mode = None + double_sided = true` still applied for parity-flip handling under `UpAxis::PlusZ`/`PlusX` — orthogonal to the per-frame recompute call.

  **Held design calls (no flip; passing recommendations from initial post):** `replay_step` over `step` for trajectory capture (avoids autograd-tape ceremony in examples); all-vertices in bevy::Mesh (interior unindexed; trivial 1:1 update path; ~24 bytes/vertex memory cost negligible at PR2 scale); in-place POSITION mutation via `Mesh::insert_attribute` (avoids per-frame `bevy::Mesh` rebuild allocation); `Time<Real>` + clamp-at-end (no looping; pause/scrub deferred until a row demands them); `OrbitCameraPlugin` left to consumer (mirrors cf-bevy-common's separation).

  **Atomic crate shape:** new `sim/L1/sim-bevy-soft/` workspace member, four src files: `Cargo.toml` (tier=L1 metadata, `publish = false`, lints mirroring sim-bevy), `src/lib.rs` (module decls + prelude), `src/mesh.rs` (`build_soft_mesh` + `apply_soft_positions` free fns), `src/trajectory.rs` (`Trajectory` Component with `#[require(Mesh3d)]` + `frame_index_at` + `duration_secs` methods + `step_replay` system), `src/plugin.rs` (`SoftBodyVisualPlugin` wires `step_replay` to `Update`, inits default `UpAxis` Resource). Workspace `Cargo.toml` adds member + `[workspace.dependencies]` entry. CI workflow `.github/workflows/quality-gate.yml` `tests-debug` adds `-p sim-bevy-soft` and updates the comment block (sim-bevy-soft tests are pure-function, same shape as cf-viewer + cf-bevy-common — no `App::new()` / window spin-up). 7 files, +400+ LOC, 13 unit tests (6 mesh + 6 trajectory + 1 normals-recompute).

  **xtask grade A across the board** (Coverage skipped, Documentation A, Clippy A, Safety A, Dependencies A, Layer Integrity skipped per L1 profile, WASM Compat skipped per L1 profile, API Design pending manual review). Layer Integrity profile note: L1 tier crates skip the strict banned-prefix + dep-count gate that L0 crates enforce — this is documented at `xtask/src/grade.rs::profile_for_crate` (sister of cf-viewer / cf-bevy-common's `applies_to_crate` exemption shape, but routed via the L1 tier metadata rather than an explicit name match).

  **Pre-commit fmt-`--all` gotcha 6-for-6** — 6th consecutive Rust-touching commit (3 + 4 + 5 + 6 + C2b + C2c) bit by per-crate-clean-but-`--all`-flagged drift. C2c's drift: a 5-element vec literal in `mesh.rs::tests::build_soft_mesh_flips_winding_under_plus_z` reflowed from one line to three. Per-crate `cargo fmt -p sim-bevy-soft -- --check` clean; `cargo fmt --all -- --check` flagged it. **Calibrated rule stands**, banked one more time at `project_git_and_hook_gotchas`. Six-in-a-row makes the `--all` rule effectively load-bearing for any Rust-touching commit; treat as a per-commit gate.

  **xtask clippy F → A fixup (1 round):** initial grade flagged F on Clippy with "0 warnings, 2 unjustified allows." Recon at `xtask/src/grade.rs:1637 count_unjustified_clippy_allows` revealed the rule: `#[allow(clippy::*)]` needs either an inline `// reason` on the same line OR a regular `//` comment within the preceding 1-3 lines (NOT a `///` doc comment, NOT a `//!` module comment). My initial `frame_index_at` + `duration_secs` allow blocks lacked justifications — `///` doc comments above were directly preceding but don't count. Adding two regular `//` comment justifications per allow block fixed it (sister of any future xtask-grade-clippy-F-on-allows session: search for `#[allow(clippy::` and verify each has a non-doc comment within 3 lines above OR an inline tail).

  **Banked at this iter (cross-arc-relevant patterns):**
  - **(a) cf-bevy-common precedent extended one level up** — when factoring a workspace support crate that owns rendering primitives (Mesh build, position update), declare minimum bevy features for what the crate's source code directly references and let consumers bring rendering pipeline features additively. The "mirror the workbench" anti-pattern (copying sim-bevy's full ~14-feature set) bloats build closures and conflates support-library shape with workbench-plugin shape. Verifiable at author-time: `cargo build -p <crate>` with the minimum set succeeds iff the crate doesn't reference omitted-feature types.
  - **(b) `#[require(Component)]` annotation expresses cross-Component dependencies at the type level** — Bevy 0.18's `#[require(...)]` on a Component declaration ensures the required Component is auto-spawned with default value if not user-supplied. Used by Mesh3d itself (`#[require(Transform)]` per `bevy_mesh-0.18.1/src/components.rs:97`). For sim-bevy-soft: `#[require(Mesh3d)]` on `Trajectory` means "Trajectory is meaningless without a Mesh3d" is structural, not via a separate marker Component. Reusable for any future per-entity sister-component pair.
  - **(c) xtask Clippy unjustified-allows rule** — `#[allow(clippy::*)]` requires a regular `//` comment justification within 3 lines preceding OR an inline `// reason` on the same line; `///` doc comments and `//!` module comments don't satisfy the rule. When clippy grade flags F with "0 warnings, N unjustified allows", grep for `#[allow(clippy::` and verify each has a non-doc comment justifying it. Documented at `xtask/src/grade.rs:1716-1721`.

  **PR2 commit count update (iter-9, post-C2c):** ~21 commits = C1 + C1-N+1 + C2a + C2a-N+1 + iter-7 correction + C2b + C2b-N+1 + iter-8 banking + C2c + C2c-N+1 + iter-9 banking + C2d + 4 rows × 3-pass = 12 + ~12 = ~24. Above iter-8's ~20 estimate by ~4 (the iter-banking commits banking N+1 architectural decisions; iter-7's pattern banked iter-8 (b) holds — every architectural-shape commit has +1 cold-read commit AND +1 inventory-banking commit).

  **N+1 cold-read findings (7 doc-rot themes consolidated, ~12 individual edits):** sister of iter-8's "code-moves-to-shared-crate doc-rot" — same pattern at the **"new-shared-crate-births-with-multiple-modules" abstraction layer**: docstrings written at author-time leak design-call provenance, internal arc planning, and overstated optimization claims into surface that should describe the code's behavior to a fresh reader. Themes:

  1. **(β) symbol everywhere** — Cargo.toml description, `lib.rs:1`, `lib.rs:8`, `trajectory.rs:12` (capture path opening). Refers to inventory iter-7 design-call labeling (β was the second of three Bevy-mode shape options considered: α live-solver-in-Bevy, β trajectory-replay, γ hybrid). Opaque cargo-cult to fresh consumers; replaced with self-contained descriptions.
  2. **"Q5 trajectory-replay (β) lock" reference** at `trajectory.rs:12` capture-path opening — same family as (1) but additionally references "Q5" (inventory iter-7 question label). Reframed to "The headless solver harness builds the trajectory" — describes WHAT, not the design-call provenance.
  3. **"PR2 rows 12, 13, 14, 18 + PR3 row 20" reference** at `trajectory.rs` multi-soft-body section — internal arc planning leaked into library docs. Reframed to describe the multi-body story directly: per-entity Component shape supports any number of soft bodies; no global "current soft body" Resource to migrate.
  4. **"Q4 v1 lock" reference** in test docstring at `trajectory.rs::frame_index_at_clamps_past_duration`. Same opaque-provenance pattern; replaced with "(clamp, not loop)".
  5. **lib.rs misdescriptions of `step_replay`** — "stepping a frame counter" (no explicit counter exists, frame index is computed from `Time<Real>` directly each tick); "Reuses `OrbitCameraPlugin` for camera input" (this crate does NOT consume `OrbitCameraPlugin` — only `UpAxis`; consumers wire the camera plugin themselves); submodule doc said `step_replay` "advances the per-entity frame index" without mentioning it ALSO writes positions + normals. All three corrected.
  6. **mesh.rs "in-place position update" overstatement** — module-level docstring + per-fn docstring claimed "in place" but `apply_soft_positions` allocates a fresh `Vec<[f32; 3]>` per call (the per-fn docstring's footnote already acknowledges this — internally inconsistent). Reframed to "per-frame position + smooth-normal write" + "overwrites" instead of "in place." The `Mesh::attribute_mut` future-optimization escape hatch retained.
  7. **mesh.rs test fixture false-anchor** — `single_tet_positions` docstring claimed "Matches `sim-soft::SingleTetMesh::vertices`'s right-handed canonical layout"; verified at `sim/L0/soft/src/mesh/single_tet.rs:34-39` that `SingleTetMesh` uses 0.1-edge tet (decimeter-edge, walking-skeleton spec §2), not 1.0-edge as the test fixture; and `vertices` is a private field anyway. Replaced with self-anchored description.

  **Banked at this N+1 (cross-arc-relevant pattern):**
  - **(d) "new-shared-crate-births-with-multiple-modules" doc-rot variant** — when authoring a new crate from scratch (vs migrating extracted code into a new crate per iter-8's variant), docstrings still leak author-time design-call provenance ("(β)", "Q5", "PR2 row N") + internal arc planning ("PR2 rows X, Y, Z") + overstated optimization claims ("in-place" when actually allocating). Iter-9's 7-theme finding exceeds iter-8's 5-fixup count by ~40%, suggesting **pattern scales with module count + architectural surface**: more modules = more places to leak design-call provenance, more architectural-decision-history references in module-level docs. Going-forward N+1 cold-reads should plan for `~max(5, 2 × module_count)` items as a conservative budget. Fresh-consumer-first wording: "what does this code do" beats "why was it designed this way" in module + function docs; design-call provenance belongs in the inventory log + commit messages, NOT in the source.

  **Next iteration entry point:** C2d — sim-bevy migration to cf-bevy-common. Recon: read `sim/L1/bevy/src/{camera,plugin,lib}.rs` for migration surface + verify sim-bevy examples don't import `OrbitCamera` by absolute path. Resolve the three reconciliation points banked iter-7: (i) defaults → cf-viewer's broader bounds win, sim-bevy's `spawn_orbit_camera` overrides via `with_*` builders so the actual spawned view is unchanged; (ii) pan guards → cf-viewer's `normalize_or_zero`-protected version (already in cf-bevy-common); (iii) `framing_for_aabb` → shared, sim-bevy may opt-in. Atomic commit: delete `sim/L1/bevy/src/camera.rs` (~245 LOC); update `sim/L1/bevy/src/{lib,plugin}.rs` to re-point at `cf_bevy_common`'s `OrbitCamera` + `OrbitCameraPlugin`; add `cf-bevy-common` dep to `sim-bevy/Cargo.toml`. Existing sim-bevy tests must stay green.

- **2026-05-06 (iter 10 — C2d sim-bevy migration shipped via β re-export shim + builder lift; PR2 foundation phase complete):** Final foundation commit closing the cf-bevy-common factor-out arc. Pre-author recon flagged a blast-radius surprise the iter-7 plan didn't anticipate — 175 absolute-path imports of `sim_bevy::camera::*` workspace-wide — and user flipped one default (β re-export shim, not the prompt-literal α delete-entirely + 175-file rewrite) per `feedback_genuine_agreement` after I surfaced the tradeoff.

  **Locked design calls (C2d in-arc):**

  1. **Migration shape β: thin re-export shim, NOT delete-entirely + 175-file import rewrite.** `sim/L1/bevy/src/camera.rs` becomes 12 LOC (10 doc + 1 blank + 1 `pub use`): `pub use cf_bevy_common::camera::{OrbitCamera, OrbitCameraPlugin}` + module doc explaining the re-export. The architecture is fixed by sharing one OrbitCamera implementation across cf-viewer + sim-bevy + sim-bevy-soft; the namespace `sim_bevy::camera::*` is a stable public path forwarding to cf-bevy-common. All 175 example imports keep working unchanged. Re-exports are canonical Rust idiom (e.g. nalgebra → simba) — not a back-compat hack.

  2. **Lift `new()` + `with_target` + `with_distance` (clamped) + `with_angles` (clamped) to cf-bevy-common as inherent methods on `OrbitCamera`.** 16+ active call sites (sim-bevy's `scene.rs` + `examples.rs` + 14 `sim/L1/bevy/examples/*.rs` + `plugin.rs::spawn_orbit_camera`) keep their fluent builder API. Required by Rust's orphan rule: inherent methods can't be added to a re-exported type, so the alternatives (extension trait + 16-site `use OrbitCameraExt;`, OR struct-update migration at all 16 sites) cost more churn for less benefit. Cf-bevy-common gains a small ergonomic surface that future consumers benefit from.

  3. **Default-value reconciliation: cf-bevy-common's broader bounds win** (per iter-7 Q4 lock). pan_speed 0.01 → 0.002 (5× slower; better for inspection feel), min_distance 0.5 → 0.1, max_distance 100 → 1000. Behavioral impact limited to the `SimViewerPlugin::spawn_orbit_camera` path. ExampleScene and the 14 sim-bevy examples set their own pan_speed/distance/min/max explicitly via field assignment after the builder chain, and are unaffected.

  4. **`OrbitCameraPlugin` added to sim-bevy's prelude** alongside `OrbitCamera` (which was already there). Closes a discoverable gap: 170+ workspace examples were importing `OrbitCameraPlugin` via the absolute path `sim_bevy::camera::OrbitCameraPlugin` because it wasn't in the prelude. New code can use `sim_bevy::prelude::*` cleanly.

  5. **`spawn_orbit_camera` moves into `plugin.rs` as a private fn** (verified zero external callers via grep). Narrower public surface; sim-bevy-specific MJCF-scene defaults (target Y=1.0, distance=8.0, angles 0.8/0.6) stay where the only caller is.

  **Held design calls (no flip):** internal sim-bevy callers import from `cf_bevy_common::camera::*` directly (skipping the local re-export — one canonical path everywhere); 3 sim-bevy `camera.rs` unit tests deleted as duplicates of cf-bevy-common's existing 9 tests + 4 new builder tests; cf-viewer needs no touchups (already uses struct-update syntax in main.rs); ExampleScene + spawn_example_camera leave as-is (B-1 lift keeps them working unchanged).

  **Atomic commit shape (`ee203127`):** 8 files changed, +124/-247 LOC. cf-bevy-common/src/camera.rs gains 4 builders + 4 tests (15 → 19 tests); sim/L1/bevy/src/camera.rs slims from 245 LOC (duplicated OrbitCamera + Plugin) to 14 LOC re-export shim; sim/L1/bevy/src/{plugin.rs, lib.rs, scene.rs, examples.rs} switch to cf_bevy_common imports; sim/L1/bevy/Cargo.toml adds cf-bevy-common workspace dep. cf-bevy-common 19/19 unit tests + sim-bevy 62/62 unit tests + 29 doctests green. Spot-build of 6 example crates (3 from `examples/fundamentals/` using `sim_bevy::camera::OrbitCameraPlugin`, 3 from `sim/L1/bevy/examples/` using `OrbitCamera::new().with_*`) confirms β re-export resolves transparently.

  **xtask grade A** for both crates — sim-bevy's Layer Integrity skipped per L1 tier; cf-bevy-common's per path-exemption at `xtask/src/grade.rs:2228`. **No new exemption needed for sim-bevy** — iter-7 speculated "Layer-integrity exemption likely needed (mirrors cf-viewer `d2eb7e89`)"; recon at C2d showed L1 tier already skips the strict banned-prefix + dep-count gate, so adding `cf-bevy-common` as a dep was a non-issue.

  **Pre-commit fmt-`--all` gotcha 7-for-7** — 7th consecutive Rust-touching commit (3 + 4 + 5 + 6 + C2b + C2c + C2d) bit by per-crate-clean-but-`--all`-flagged drift. C2d's drift: rustfmt sorted the prelude `pub use` statements alphabetically when I added the cf_bevy_common line, which orphaned the next-line `// Model/Data architecture` comment. Caught at fmt --all step + cleaned by adding an adjacent comment for the cf_bevy_common re-export. **Calibrated rule stands**.

  **Banked at this iter (cross-arc-relevant patterns):**

  - **(a) β re-export shim for shared-crate factor-outs with high-import-count namespaces** — when migrating a duplicated module to a shared crate AND the original namespace has many absolute-path imports, the right shape is often a thin re-export shim (`pub use shared_crate::module::{...}` + module-level `//!` doc) preserving the namespace as a stable forwarding path, NOT delete-entirely + bulk-rewrite-imports. Architectural goal (one implementation) is met by the re-export; the namespace is a path, not architecture. Re-exports are canonical Rust idiom (nalgebra → simba). **Trigger: count absolute-path imports BEFORE choosing α vs β; if >50 files, default β**. Sister of `feedback_no_rationalizing_doc_drift` inverted: this is "don't rationalize a 175-file diff when 4 lines achieve the same architectural outcome".

  - **(b) Builder-lift-as-orphan-rule-workaround at shared-crate migration time** — Rust's orphan rule blocks adding inherent methods to a re-exported type. When the original crate had inherent-method builders (`with_*`, `new()`) and you're migrating via re-export shim, **lift the builders to the shared crate at migration time**. Cost is small (~30 LOC + tests in the shared crate); benefit is preserving fluent API at every call site without orphan-rule workarounds (extension traits, struct-update migration). Sister of (a): both choose "low-churn migration" over "ideal end-state at maximum diff cost".

  - **(c) Speculative pre-emptive-workaround predictions — verify-before-implementing audit** — iter-7 banked two speculative C2d predictions that BOTH proved wrong at recon time: (i) "view stays unchanged" (assumed `spawn_orbit_camera`'s builder chain overrode all reconciled fields; recon showed it overrides only target/distance/angles, NOT pan_speed/min/max — so pan_speed DOES shift 5× for the spawn path); (ii) "Layer-integrity exemption likely needed (mirrors cf-viewer `d2eb7e89`)" (assumed sim-bevy needed an explicit exemption like cf-viewer's; recon showed L1 tier already skips strict gate via tier metadata, not name-match). Both were architecturally sensible at iter-7 time but factually wrong at recon time. **Pattern: at deep-architecture sessions, mark speculative claims explicitly as "verify at author-time" rather than baking them into the locked-in plan.** Sister of `feedback_spike_before_trust_analytical` "Architectural claims about code structure" — same anti-pattern at the predict-the-recon-result abstraction level.

  - **(d) Default-override-overlap precision when reconciling shared-crate defaults** — when reconciling default values across consumers via "broader bounds win + consumer's builder chain overrides them", precisely compute which fields the builder chain actually touches. A `with_target().with_distance().with_angles()` chain on `OrbitCamera` overrides 4 fields (target, distance, azimuth, elevation) — leaves the other 7 (pan_speed, orbit_speed, zoom_speed, min_distance, max_distance, min_elevation, max_elevation) at the post-migration default. Easy to forget at deep-architecture time when field count is high. **Pattern: list fields-overridden vs fields-defaulting at reconciliation time, not just "the builder chain handles it"**. Sister of (c): same root cause — speculation about builder behavior without enumerating the field-by-field overlap.

  - **(e) Drift-count-scales-with-surface confirmed via smallest-yet datapoint** — iter-9 banked the observation that cold-read doc-rot count scales with module count + architectural surface. C2d shipped a re-export shim + builder lift (NOT a new crate, NOT new modules). First-pass cold-read found 1 theme; second-pass found 1 more (style consistency). Total 2 themes across both passes. Iter-9's `~max(5, 2 × module_count)` budget gives `max(5, 0) = 5` upper bound; actual was 2. **Surface-size is the right driver; new-module-count is one component but architectural-shape novelty is another.** C2d had architectural novelty (β shape decision) but minimal new surface (12-LOC shim, of which only 1 is code) — drift count tracks the surface, not the architecture.

  **PR2 commit count update (iter-10, post-C2d):** ~26 commits = C1 (`71e9bb77`) + C1-N+1 (`839b7a40`) + C2a (`b7758457`) + C2a-N+1 (`0dfff492`) + iter-7 correction (`93a68373`) + C2b (`7905c07c`) + C2b-N+1 (`97ab41a2`) + iter-8 banking (`5b75f909`) + C2c (`87d6d165`) + C2c-N+1 (`3b68b60c`) + C2d (`ee203127`) + C2d-N+1 (`e372d4c4`) + C2d-N+2 (`12013685` second-pass cold-read) + iter-10 banking (this commit) + 4 rows × 3-pass = 14 + ~12 = ~26. ~+2 over iter-9's 24 estimate (the second-pass cold-read at `12013685` was unanticipated; user invoked `feedback_thorough_review_before_commit`'s "N+1 user-initiated passes always find something" pattern explicitly post-C2d-N+1).

  **N+1 cold-read findings (2 themes total across two passes):**

  1. **First pass (`e372d4c4`) — `sim/L1/bevy/src/lib.rs` `# Architecture` diagram boxed Camera as a sim-bevy-internal module** alongside Systems + Gizmos. After C2d, OrbitCamera + OrbitCameraPlugin live in cf-bevy-common; sim-bevy re-exports them via `sim_bevy::camera::*` and the prelude. The diagram-as-drawn was misleading without context. Fix: one-line note immediately after the diagram pointing at cf-bevy-common as single source of truth, with intra-doc links to `[`crate::camera`]` (the re-export shim) and `[`crate::prelude`]`. Diagram preserved (Camera IS still part of sim-bevy's user-facing surface via the re-export); dependency relationship made explicit.

  2. **Second pass (`12013685`) — `sim/L1/bevy/src/camera.rs`'s module-level doc used literal-URL form** (`(../../cf_bevy_common/camera/index.html)`) for the cf-bevy-common reference, while my just-shipped lib.rs note used intra-doc form (`[`crate::camera`]`). Mixed styles within the same C2d arc, and the literal-URL form is fragile (breaks under doc-build layout changes; doesn't track renames; rustdoc emits no warning when the URL is wrong). Fix: switch to intra-doc form `[`cf_bevy_common::camera`]` for both robustness + style consistency.

  **Banked at this N+1 (cross-arc-relevant patterns):**

  - **(f) Intra-doc form > literal-URL form for cross-crate doc references** — `[\`crate_name::path\`]` auto-resolves under any rustdoc build setup AND tracks renames; literal URL `(../../crate_name/path/index.html)` is fragile + uncatchable by `RUSTDOCFLAGS=-D warnings`. When cross-referencing a workspace dep's docs, default to intra-doc syntax. Sister of `feedback_no_rationalizing_doc_drift` — "URL works locally" rationalizes a fragile form when the robust form is one keystroke away.

  - **(g) Second-pass cold-read empirically validated** — `feedback_thorough_review_before_commit` patterned "N+1 user-initiated passes always find something". C2d's user-invoked second pass (post-C2d-N+1) found a real style-consistency drift the first pass missed. Confirms the pattern's predictive value: **at architectural-shape commits, default to two cold-read passes, not one**. Cost is small (one extra Read + Edit + commit cycle); finding rate is non-zero (1/1 in this case).

  **Next iteration entry point:** Row 12 — PR2's first example row (the soft-body + rigid-primitive contact arc opens). **New session recommended for phase transition** (foundation phase → example-rows phase): different work mode (architectural-recon-heavy vs example-implementation-heavy), and this session has accumulated ~30+ files of foundation-phase context that row 12 doesn't need. Recon for row 12: read this file's row 12 spec section + `sim/L0/soft/src/contact/`'s post-C1-trait-unification surface + `sim/L1/sim-bevy-soft/src/{trajectory,mesh,plugin}.rs` for the visualization-side consumer interface. Cadence: 3-pass per row (commit + N+1 cold-read + N+2 visual review or JSON-collapse per `feedback_visual_pass_collapses_for_json_rows`).

- **2026-05-06 (iter 11 — row 12 soft-drop-on-plane shipped + 3 sim-bevy-soft foundation patches surfaced + RENDER_SCALE pivot + N+1 cold-read fixup; PR2 first example row complete):** Row 12 shipped end-to-end across **10 commits** in a single multi-hour session — 1 row commit + 6 visual-review iterations + 3 sim-bevy-soft foundation patches surfaced by the example. The 3-pass cadence (commit + N+1 cold-read + N+2 visual review) collapsed: the N+2 visual review WAS the iterative session (`038a8ab7` lighting + `886e6ba3` polish + `e4120b47`/`ba8bcc76` slow-motion + `fe6ff49e` RENDER_SCALE pivot all happened during the user's eyes-on-pixels review, with each visual finding driving the next commit). The N+1 cold-read pass (`49f839c8`) ran at the end and found 8 themes / ~16 individual edits — within iter-9's `~max(5, 2 × surface units)` budget when counting design pivots as surface units.

  **Locked design calls (row 12 in-arc):**

  1. **Visual-mode opt-in via `CF_VISUAL=1`** environment variable (NOT CLI flag) — minimal one-line check (`std::env::var("CF_VISUAL").is_ok()`), composes cleanly across `cargo run -p ...`, no clap dep. Documented in `examples/EXAMPLES.md` as the workspace convention for sim-bevy-soft consumers; rows 13/14/18 inherit. `CF_*` prefix matches `cf-viewer` / `cf-bevy-common` / `cf-design` namespace.

  2. **Render-scale `100×` lift** for visual entities (`fe6ff49e`) — the soft mesh's `Transform::from_scale(100)`, plane half-size and camera distance + target all scaled by `RENDER_SCALE`. Bevy 0.18's pipeline defaults (near plane `0.1 m`, OrbitCamera `min_distance = 0.1 m`, AmbientLight brightness, depth precision) are tuned for human-scale (1 m+) scenes; at sim-soft's cm-scale, intermediate workarounds (custom `Projection::Perspective` near override, `min_distance = 0.05`, `pan_speed = 0.0`, `cull_mode: None` diagnostic) chased symptoms but never converged. RENDER_SCALE lifts the rendered scene to where defaults Just Work. **Headless asserts + PLY emit are scale-invariant** (operate on unscaled physics positions in `DropSnapshot`); RENDER_SCALE is purely visualization-layer. Row 13/14/18 should adopt the same pattern by default.

  3. **`SLOW_MO_FACTOR = 10×` replay default** — analytic time-to-impact for sphere-bottom is `~89 ms`, blink-and-miss-it at 1× wall-clock; `10×` puts the freefall + contact-onset arc at `~890 ms` (clearly observable, contact-onset reads as a distinct beat) while the settle-and-rest phase fits under `9 s`. Pure visualization knob; consumers override per-spawn via `Trajectory.dt` (the `EXAMPLES.md` convention text frames `1×` as the default + slow-motion as the consumer override).

  4. **Coral PBR base color + on-screen Text HUD** for the soft mesh — coral (`Color::srgb(1.0, 0.55, 0.4)`, perceptual_roughness `0.5`, metallic `0.05`) reads cleanly against Bevy's dark-gray clear color, ASCII `|`-separated controls HUD at `bottom-left` lights yellow on dark gray. ASCII `|` because the default font (`default_font` Cargo feature) doesn't render U+00B7 middle-dot at the chosen 14 px size — surfaced by missing-glyph boxes during visual review.

  5. **3 sim-bevy-soft foundation patches surfaced by row 12 visual review:**

      - `38aa7a78` — **`ReplayEpoch` per-entity component** that captures the wall-clock at the first `step_replay` tick instead of `App::new()` startup; auto-added via `Trajectory`'s `#[require(ReplayEpoch)]`. Without this, `DefaultPlugins` startup time (`~1-2 s` on first run) consumed the 1-s trajectory budget before the user saw frame 0.

      - `9a2841c4` — **`reset_replay_on_keypress` system** (KeyR) that clears every entity's `ReplayEpoch.epoch_secs = None` so the next `step_replay` tick captures a fresh epoch. Wired into `SoftBodyVisualPlugin` as `(reset_replay_on_keypress, step_replay).chain()` so reset takes effect on the same frame the key is pressed.

      - `cc2e6324` — **`compute_area_weighted_normals` swap** (replacing `compute_smooth_normals`) in `build_soft_mesh` + `apply_soft_positions`. Bevy 0.18's corner-angle-weighted variant has a hardcoded `EPS = f32::EPSILON ≈ 1.2e-7` gate on `(edge_a² × edge_c²)`; at `3 mm` cell size the squared-edge product is `~8e-11`, so EVERY per-vertex weight zeros out and PBR sees `NdotL = 0` everywhere (sphere reads as uniformly dark). Existing test fixture used `1 m`-edge tetrahedra (passes the EPS gate) and didn't catch this. Added `build_soft_mesh_normals_unit_magnitude_at_sim_soft_scales` regression test that scales the fixture to `1 cm` edges and asserts every indexed-vertex normal has unit magnitude.

  **Iterative-visual-review workflow (banked as pattern (h) below):** the session walked through several intermediate visual-review states before converging on RENDER_SCALE — dark sphere (lighting tune `038a8ab7`), too-fast freefall (slow-mo bump `e4120b47`), HUD missing-glyph boxes (ASCII `|` swap `886e6ba3`), pan-into-sphere artifact (intermediate min_distance + pan_speed = 0 fix, then superseded by RENDER_SCALE), near-plane clipping at zoom (intermediate `Projection { near: 0.001 }` override, then superseded by RENDER_SCALE). Each intermediate fix shipped doc claims that the RENDER_SCALE pivot then orphaned — the N+1 cold-read found 8 themes consolidating these.

  **N+1 cold-read findings (8 themes total / ~16 individual edits):**

  1. **Sphere-center vs sphere-bottom analytic** (5 places: module docstring, anchor 5 docstring, N_STEPS const, verify_contact_engagement assert message, README anchor 5 prose). Claimed `t_c = sqrt(2h/g) ≈ 100 ms ≈ step 100` with `2.5×` headroom, but the gate measures sphere-bottom (`t_c = sqrt(2(h-R-d̂)/|g|) ≈ 89 steps`, captured 88, headroom `~2.8×`). Aligned all to sphere-bottom.
  2. **Pre-RENDER_SCALE camera + plane sizes** in module docstring + README — claimed `0.15 m` distance + `4 cm × 4 cm` plane, post-RENDER_SCALE values are `15 m` distance + `8 m × 8 m` plane (4 m half-size per Bevy `Plane3d::new`) + camera target `(0, 1.1, 0)` Bevy. Updated.
  3. **Stale orphan camera comment block** in `setup_visual_scene` — duplicate paragraph survived the RENDER_SCALE rewrite. Collapsed.
  4. **N_STEP_FIRST_CONTACT_REF formula drift** — written `sqrt(2(h-R)/|g|)` but should be `sqrt(2(h-R-d̂)/|g|)` (numerical value `0.039` IS correct).
  5. **README "blue PBR sphere"** — pre-coral-color drift. Updated to "coral PBR sphere".
  6. **README "middle-drags to pan"** — pan is right-click drag (per OrbitCamera source); table was already correct, prose contradicted it. Removed redundant prose.
  7. **EXAMPLES.md convention text** — claimed "1× wall-clock + tracks Time<Real>" but row 12 ships `10×` slow-motion + per-entity ReplayEpoch (epoch-relative). Reframed to match ReplayEpoch foundation + slow-motion-as-consumer-override.
  8. **step_replay docstring stale "Add a custom replay-clock Resource"** suggestion — pointed at a Resource solution that ReplayEpoch (per-entity Component, shipped at 38aa7a78) has since obviated. Reframed.

  **Banked at this iter (cross-arc-relevant patterns):**

  - **(h) Iterative-visual-review doc-rot variant** — when a session iterates through several visual-review attempts (lighting tune, near-plane workaround, min_distance cap, pan-disable, render-scale pivot), early intermediate claims linger in docstrings + README prose AFTER the design pivots away from them. Sister of iter-7's "code-moves-to-shared-crate" + iter-8's "new-shared-crate-births" + iter-9's "new-shared-crate-births-with-multiple-modules" patterns at the **in-session-design-pivot abstraction layer**. **Trigger: cold-read budget for sessions with ≥3 design pivots scales by the pivot count** — row 12's session had 5+ intermediate workarounds replaced by RENDER_SCALE alone, each leaving doc traces. 8 themes is consistent with iter-9's `~max(5, 2 × surface units)` budget if we count design pivots as surface units.

  - **(i) Headless-vs-visual review consolidation** — the iter-2-locked split (PLY + JSON only / Bevy real-time / per-tier visualization assignment) survives the row 12 iteration intact, plus a new sub-pattern: **for Tier 4 + Tier 6 rows where visual review is real (per `feedback_visible_contacts`), the iterative visual-review IS the N+2 review pass** — the user's eyes-on-pixels review happened in the same session as the headless code review (commit + N+1 cold-read), with each visual finding driving the next commit. The 3-pass cadence collapsed naturally; no separate N+2 commit needed. Banked: for Tier 4/6 rows shipping with `CF_VISUAL=1` + iterative visual review in-session, the cold-read fixup commit at end of session IS the N+1 pass; the visual-review pass is implicit in the iterative shipment.

  - **(j) Foundation-patch overhead per example row** — row 12 surfaced 3 sim-bevy-soft foundation patches (`38aa7a78`, `9a2841c4`, `cc2e6324`). All three were genuine bugs the foundation phase didn't catch because the test fixtures used 1-m-edge tetrahedra (passes Bevy's EPS gate) and the foundation didn't have a real consumer to surface the App-startup-vs-replay-budget interaction. Banked observation: **first-consumer rows of newly-shipped foundation crates surface ~3 foundation patches per row, even after a thorough N+1 + N+2 cold-read on the foundation itself**. Trigger: budget `~3 foundation-patch commits` per first-consumer row; subsequent rows that consume the same foundation should surface fewer (rows 13/14/18 inherit the row 12-fixed foundation).

  **PR2 commit count update (iter-11, post-row-12):** ~38 commits = 15 foundation-phase commits (14 per iter-10's count + post-iter-10 cold-read fixup `8401eed6` not budgeted in iter-10) + iter-11 banking (this commit) + row 12: `4b745f31` (C12) + `38aa7a78` (foundation: ReplayEpoch) + `ba8bcc76` (slow-mo 5×) + `9a2841c4` (foundation: R-key) + `e4120b47` (slow-mo 10× + R-key README) + `886e6ba3` (coral + HUD) + `cc2e6324` (foundation: area-weighted normals) + `038a8ab7` (lighting tune) + `fe6ff49e` (RENDER_SCALE pivot) + `49f839c8` (N+1 cold-read fixup) = 10 row-12-related commits. Row 12 itself = 10 commits, +1 iter-11 banking = 11; foundation 15 + 11 = 26 total at this point. Remaining: 3 rows (13, 14, 18) × ~6-8 commits each = ~18-24 more (assuming +1-2 foundation patches per row per pattern (j) above). Total PR2 estimate: ~44-50 commits. ~+18 over iter-10's 26 estimate; the increase is from pattern (j) (foundation-patch overhead per example row, originally not budgeted) + iterative-visual-review iteration count exceeding initial single-pass-per-row assumption.

  **Pre-flight: build, test (sim-bevy-soft 17/17), fmt --all, clippy -D warnings, cargo doc -D warnings, xtask grade A on automated criteria** (run after each row-12-related commit). **Push to origin recommended at row 12 close** since 11 unpushed commits accumulate risk; not done (per `feedback_no_push_ultrareview` deferred to user discretion). Row 13 should start fresh with branch pushed.

  **Next iteration entry point:** Row 13 — PR2's second example row (`hertz-sphere-plane`, the canonical Hertzian indentation analytic gate per inventory Tier 4). **New session recommended** for phase transition: this session accumulated full row 12 context (lighting/camera iteration + 3 foundation-patch debug paths + RENDER_SCALE pivot reasoning) that row 13 doesn't need. Row 13 inherits the row 12-fixed foundation (ReplayEpoch + R-key reset + area-weighted normals) AND the visualization conventions (RENDER_SCALE 100×, SLOW_MO_FACTOR knob, CF_VISUAL=1 opt-in, coral PBR + HUD pattern). Recon for row 13: read this file's row 13 spec section + `sim/L0/soft/tests/hertz_sphere_plane.rs` (Hertzian internal fixture) + row 12's `examples/sim-soft/soft-drop-on-plane/` as the immediate precedent. Cadence: 3-pass per row (commit + N+1 cold-read + N+2 visual review); per pattern (i) the visual review may collapse into the iterative session for this Tier 4 row.

  **Push branch to origin before starting row 13 session** — `git push origin dev` (11 unpushed commits at branch tip `a86f9320` after this iter-11 banking commit lands). Recommended per `feedback_push_feature_branch_default`.

- **2026-05-06 (iter 12 — row 13 hertz-sphere-plane shipped + 2 cf-view foundation patches surfaced + 9-theme N+1 cold-read pre-commit + 3-iteration visual review; PR2 second example row complete):** Row 13 shipped end-to-end across **3 commits** (initial + annulus visibility/HUD fixup + camera-revert + README zoom guidance). The 3-pass cadence collapsed differently from row 12 — N+1 cold-read ran **pre-commit** (per `feedback_thorough_review_before_commit`'s "thorough re-read before commit" preference applied this iteration); visual review iterations ran in-session per pattern (i). Pattern (j) prediction held: 2 cf-view foundation patches surfaced (down from row 12's 3 sim-bevy-soft patches), consistent with "decreasing as foundation matures" — but at a different abstraction layer (cf-view, not sim-bevy-soft, which row 13 inherited stable from row 12).

  **Locked design calls (row 13 in-arc):**

  1. **`a_FEM` headline gate, `δ_FEM` diagnostic-only** — the Hertzian fixture's "Plan change 2" reframe verbatim. Penalty compliance band makes Hertz indentation `δ_Hertz` structurally unreachable; the FEM finds the correct contact-patch radius `a_FEM` to within mesh-bound discretisation error. Both reported in stdout + JSON; only `a_FEM` asserted at `rel_err_a < 20%` finest-level + monotonic + Cauchy `< 1`. Captured `15.84%` at h/4 (~4 pp headroom over gate); 45 active pairs at h/4 (matches the Hertzian fixture's docstring exactly).

  2. **3-section JSON schema** — scalars (radii, errors, counts) + per-active-vertex `(r, sd, force_z)` at finest only + 200-pt Hertz analytic curve `p(r) = p₀·sqrt(1 - (r/a)²)`. plot.py PEP 723 single-file overlays per-vertex points against analytic. Per-vertex emits raw `force_z = κ·(d̂ - sd)` not pressure (Voronoi-cell-area conversion is a follow-on). Honest qualitative shape match.

  3. **Per-vertex `contact_force_z` PLY extra at finest only** — `κ · (d̂ - sd)` for active vertices, zero elsewhere. cf-view auto-colormap should render contact patch as a bright disk on the deformed sphere. Fills row 12's "contact-band membership is a row-13 Hertz-patch concern" deferral (modulo the cf-view C1 + C2 foundation patches surfaced this iteration).

  4. **Bevy static-state visualization (option F per planning)** — finest deformed sphere + coral `a_FEM` annulus + white `a_Hertz` annulus on plane. Single-frame `Trajectory` replay (no animation; clamp at end). Annuli at `plane_y + 0.005` Bevy with `unlit: true + cull_mode: None + double_sided: true` per pattern (k) below. Both annuli at the same `ring_y` (radii non-overlapping, `a_Hertz / a_FEM ≈ 1.19`, no z-fight). RENDER_SCALE = 100× lift inherited from row 12 banked pattern.

  5. **All 3 refinements (3, 1.5, 0.75 mm)** required for the convergence story (monotonic + Cauchy + finest gate). `cargo run --release` only (~2 min release; debug-mode unviable). Hertzian release-only policy mirrored.

  6. **Default-view = full-scene framing** — distance 3R, elevation 0.5 rad, target -0.5R. Patch annuli appear as tiny markers (radii 0.15-0.178 Bevy on a 1.0-Bevy sphere); user scroll-zooms to inspect. README docs the cadence explicitly. Pattern (l) banked.

  **N+1 cold-read findings (9 themes pre-commit, mostly polish):**
  1. Module docstring `n_pinned == 4` → `≥ 3` (matches assertion contract; helper picks 4 then dedups).
  2. Captured-bits "diagnostic-only assertion" wording sharpened (asserted at IV-1 rel-tol; the value is diagnostic vs Hertz analytic).
  3. Capture-provenance placeholder `<see capture provenance below>` filled with tip `fed36c62`.
  4. `RefinementSnapshot` docstring referenced removed `referenced` field (clippy-fixup-time drop not propagated to docs).
  5. `CELL_SIZE_H4` docstring "Hertzian fixture measured ~45" → "captured 45 (matches the Hertzian fixture's docstring exactly)" — empirical confirmation tightened.
  6. Stdout "boundary_partition: 4 equator pins" → "≥ 3 equator pins (helper picks 4, dedups)".
  7. Stdout "Bevy replay" → "Bevy visualization" (static, not replay; row 12 wording leaked).
  8. README orphan ratio "~96% at h" → "~97%" (561/18696 numerical correction).
  9. Cargo.toml comment refined (Tensor reaches transitively through sim-soft, not direct construction).

  Within iter-9 banked budget `max(5, 2 × surface units)`. Slightly below row 12's 8 themes — first-author rows have lower drift than design-pivot-heavy commits.

  **Visual review iterations (3 in-session per pattern (i)):**
  1. **Iteration 1 (commit `6628000c`)** — B1 pass 1 (annulus position lifted from sphere-bottom-coincident to plane-edge) + B2 (HUD top-right → top-left to fix multi-line truncation against viewport edge). User re-test: HUD ✓; rings still hidden by sphere body at oblique camera.
  2. **Iteration 2 (same commit `6628000c`)** — B1 pass 2: `cull_mode: None + double_sided: true` on ring materials + thickness 0.01 → 0.025 + camera elevation 0.5 → 0.9 rad + distance 3R → 2.5R + target -0.5R → -0.85R. User re-test: rings render correctly when zoomed in close, but sphere-occluding default view persisted. Pattern (k) banked.
  3. **Iteration 3 (commit `c6efcf66`)** — Camera revert to row-12-style full-scene framing (distance 3R, elevation 0.5, target -0.5R) + README "Reading the patch-radius rings" subsection with explicit scroll-zoom guidance. Pattern (l) banked: full-scene-first beats default-zoomed-in for sub-cm-feature rows because the small `a/R` ratio IS itself the small-strain-validity story.

  **Banked at this iter (cross-arc-relevant patterns):**

  - **(k) Bevy 2D-primitive back-face culling under axis-laying rotation** — when rotating a 2D primitive (`Annulus`, `Circle`, `Rectangle`) by `±π/2` to lay it flat on a coordinate plane, the disc's mesh-local +Z normal aligns with the world axis the rotation puts it on; depending on the camera's view direction relative to that normal, the back-face culls. Default `StandardMaterial { unlit: true, double_sided: true, cull_mode: None, ..default() }` ensures the disc renders from any camera angle. Sister of `feedback_visible_contacts` at the rendering-primitives layer. Trigger: any 2D primitive used as a flat marker on a 3D plane in sim-bevy-soft / cf-bevy-common consumers.

  - **(l) Full-scene-first default-view for sub-cm-feature rows** — when the row's headline visual feature is sub-cm (Hertz contact patch ~1.5-2 mm vs sphere 1 cm = 15-18% of body radius), the default startup view should show the FULL scene (sphere + plane + tiny patch markers) with explicit scroll-zoom guidance in the README, NOT a tight zoom that hides the spatial context. The sub-cm patch IS itself the small-strain validity story (`a/R ≈ 17.8%` < 30% Hertz domain ceiling); making it tiny on startup reinforces that pedagogy. Sister of `feedback_simplify_examples` "realistic-looking parts before combining domains" at the camera-framing layer. Trigger: rows where headline visual feature ≪ scene-bounding-feature.

  - **(m) Pre-commit N+1 cold-read for first-author rows** — `feedback_thorough_review_before_commit` says "explicit per-leaf-verdict re-read; N+1 user-initiated passes always find something." Row 12 ran the pattern as commit + N+1-fixup cycle. Row 13 pre-empted by running the cold-read **before** initial commit, catching 9 themes at the same N+1 level pre-commit instead of post-commit. **Banked observation: for first-author rows (no design pivots in-session), pre-commit cold-read is more efficient than post-commit-fixup cycle** — same finding density, one less commit. For design-pivot-heavy rows (like row 12's RENDER_SCALE pivot), post-commit fixup may still surface intermediate-state drift that pre-commit can't anticipate.

  **2 cf-view foundation patches surfaced (banked as deferred followups):**

  - **C1: `framing_for_aabb` doesn't auto-fit cm-scale meshes** — cf-view's default camera was too far for sub-cm PLYs (row 13's 2cm-bbox PLY rendered as a single dot from the default camera position). Same RENDER_SCALE=100× root cause as row 12's sim-bevy-soft foundation patches at a different abstraction layer. Fix candidates: (a) cf-view computes a render-scale from mesh bbox and applies a Transform::from_scale to the loaded mesh, or (b) `framing_for_aabb` clamps camera distance proportional to bbox max-dim with no min-distance lower-bound. (b) is cleaner per the "RENDER_SCALE-as-rendering-pipeline-default-workaround" pattern — keeps the loaded mesh in its native units.

  - **C2: Colormap default heuristic picks `divergent` for unipolar fields** — `contact_force_z` is non-negative everywhere (zero for inactive vertices, positive `κ·(d̂-sd)` for active); cf-view defaulted to `divergent` (red-blue around 0). Sequential viridis is the right pick when min ≥ 0. Fix: cf-view's auto-detection should check `min_value >= 0` (sequential) vs `straddles-zero` (divergent). One-line heuristic. **DISPROVEN at C1-fix re-test (`ca7d375c`):** post-C1 cf-view startup with row 13 PLY shows the colormap dropdown at `auto` and the auto-detect correctly picked sequential (viridis purple-to-yellow rendering). The earlier "divergent" read in the row 13 visual review was a misread of the dropdown state or a manual user toggle, not an auto-detect bug. C2 closed; no fix needed. The detect() logic in `cf-viewer/src/colormap.rs:112` is correct (`any_negative` only fires for strictly negative values).

  C1 banked + shipped at `ca7d375c` (cf-view RENDER_SCALE for sub-meter meshes). C2 disproven at the same re-test. cf-view foundation cleanup for row 13 surface complete.

  **PR2 commit count update (iter-12):** as of `73d4ce5c` (round-2 cold-read fixup), 34 commits = 26 (foundation phase complete + row 12) + row 13: `7bc7fda3` (initial) + `6628000c` (visibility + HUD fixup) + `c6efcf66` (camera revert + README zoom guidance) + iter-12 banking (`d8b8d314`) + cf-view C1 (`ca7d375c`) + iter-12 amendment (`c82e784f`, C2 disproven) + 14-theme slow cold-read fixup (`fa83de0c`) + round-2 cold-read fixup (`73d4ce5c`). **Subsequent fixup-pass commits append without invalidating this enumeration** — see `git log dev` for the current total. Remaining work: row 14 + row 18 (~3-5 commits each, sim-bevy-soft + cf-view foundation now stable) = ~6-10 more after the cold-read passes settle. Final PR2 estimate: ~40-46 commits. At or above iter-11's ~44-50 upper bound depending on cold-read decay rate (pattern (m) extension banked at this iter — fixup-driven drift requires its own follow-up read; observed decay rate so far: 14 → 8 themes per pass).

  **Pre-flight per row-13 commit: build, fmt --all, clippy --workspace -- -D warnings, cargo doc -D warnings, xtask grade A on automated criteria.** **Pushed to origin progressively** as iter-12 commits land; current `dev` tip is recorded in MEMORY.md `Resume here` header (single source of truth, robust against staleness here as new fixup commits land).

  **Next iteration entry point:** **row 14 — `compressive-block`**. cf-view foundation cleanup is complete this iteration (C1 shipped at `ca7d375c`, C2 disproven at `c82e784f`); rows 14 + 18 + 20 inherit cf-view's auto-RENDER_SCALE for any sub-meter PLYs they emit. **New session recommended** for phase transition (row 13 accumulated annulus rendering / camera framing iteration / cf-view foundation surfacing context that row 14 doesn't need). Row 14 inherits row 12-fixed sim-bevy-soft foundation + row 13-fixed annulus rendering pattern (k) + cf-view auto-scale. Recon for row 14: read this file's row 14 spec section + `sim/L0/soft/tests/penalty_compressive_block.rs` (compressive-block internal fixture) + row 12 + row 13 as immediate precedents. Cadence: 3-pass per row.

- **2026-05-06 (iter 13 — row 18 contact-force-readout shipped + 1 PROACTIVE foundation patch + ~24-theme integrated cold-read across 5 passes; PR2 fourth + last example row complete, all four rows shipped, ready for ship-time decisions):** Row 18 shipped end-to-end across **10 commits** = 2 (foundation patch: `995fb0bf` `per_pair_readout` + `ContactPairReadout` + integration test, `38cc3f42` foundation cold-read 5 themes) + 8 (example row + cold-read passes: `11cea6f1` initial + `67a5cc77` post-commit cold-read 6 themes (sign-convention drift) + `4bea4c2f` visual-review fixup 6 findings (Bevy plate alignment + HUD ASCII) + `b319f6f1` cf-view doc-gap 2 findings + `f66b347e` cf-view cold-read 3 themes + `5640cdf0` Saint-Venant inversion fix 12 locations + `b3614811` inversion-fix cold-read 1 theme (sub-micron) + `2768a3ef` end-of-session cold-read 4 themes). Row 14 (4 commits, 16 themes) banked separately; this iter focuses on row 18 + foundation.

  **Locked design calls (row 18 in-arc):**

  1. **`per_pair_readout` on `PenaltyRigidContact` (NOT `ContactModel` trait)** — inherent method on the concrete type, not promoted to trait. Reason: Phase 5 ships PenaltyRigidContact + NullContact only; the latter has no contact pairs. Future IPC contact models can either reuse `ContactPairReadout` struct or define their own. Skipped lift to trait per "Don't add features for hypothetical future requirements."

  2. **`ContactPairReadout` struct in `contact/mod.rs` (next to `ContactPair`)** — shares discoverability home with sister types `ContactGradient` / `ContactHessian`. Field shape: `(pair, position, sd, normal, force_on_soft)`. `force_on_soft: Vec3` (not `force_z: f64`) generalizes to row 20's scan-derived `MeshSdf` primitive.

  3. **Inspection contact via clone-construct (NOT `Solver::contact()` accessor)** — example builds a SECOND `PenaltyRigidContact` for post-step inspection (the original was moved into the solver). ~5 LOC duplication in the example, no Solver-side change. Skipped accessor patch per locked decisions; principled minimum-surface foundation patch.

  4. **Single-refinement n=8 (NOT three)** — row 18's headline is per-pair readout (API/structural property), not convergence (row 14 owns that). Single refinement gives 81 active pairs (max informativeness for per-pair JSON) at ~3× faster runtime.

  5. **Bit-equivalent captures with row 14** — `lambda_z_avg`, `eps`, `f_r_total` (sign-flipped), `effective_modulus` are bit-equivalent to row 14's `*_N8_REF_BITS`. Cross-row regression detection: a regression at the `(d̂, δ)` override would fail BOTH rows simultaneously.

  6. **Two-section JSON (NOT three)** — `scalars` + `pairs`. Drops row 14's `analytic` curves section; row 14 owns F-vs-ε visualization.

  7. **Per-vertex `contact_pressure` PLY extra (vs row 14's `contact_force_z`)** — uniform per-pair area approximation. Per-vertex Voronoi area banked as live followup (no current consumer).

  8. **CF_VISUAL=1 mirrors row 14 with HUD swap + visual-review divergences** — same two-plate harness, same VIZ_AMPLIFY=50× + plate-flush positioning. **HUD swapped to per-pair statistics** (max/mean/min force_z + pressure + n_active + ε + λ_z + e_eff + amplification disclosure). Visual-review fixup `4bea4c2f` introduced two divergences from row 14: **plates centered at cube's lateral COM** `(0.5m, *, 0.5m)` Bevy (NOT world origin) + **HUD ASCII-only** (NOT Unicode `ε`/`λ`/`σ`/`×`/em-dash). Both banked as row-14 back-port followup; row 14 inherits the same bugs.

  **Headline result**: F_R_total = 0.182 N at n=8 (matches row 14 `F_R_FEM_N8` exactly with sign flip); 81 active pairs (= (n+1)²); accessor-vs-manual consistency at 1e-12 rel; per-pair pressure spread 374 Pa (corner) to 2383 Pa (interior) — ~6× ratio under Saint-Venant boundary-layer pattern (interior locally uniaxial-strain → stiffer; corner locally uniaxial-stress → softer).

  **Pattern (j) refinement — proactive vs reactive foundation patches**: Row 12 surfaced 3 REACTIVE sim-bevy-soft patches (App-startup-vs-replay-budget, R-key reset, area-weighted normals); row 13 surfaced 2 REACTIVE cf-view patches (RENDER_SCALE C1, C2-disproven); row 14 surfaced 0; row 18 had 1 PROACTIVE patch (`per_pair_readout`, designed-in by inventory spec at this file's row 86 explicitly naming the missing accessor). **Refinement banked**: foundation-patch overhead is REACTIVE for first-consumer rows of newly-shipped foundation; PROACTIVE for rows whose inventory spec PRE-DECLARES the foundation gap. A row's spec saying "BLOCKED on X" predicts a proactive foundation patch at row-build time; "found X needed during build" predicts a reactive patch. Row 20's spec at row 93 explicitly says "BLOCKED on rows #15 + #16 + #18 + #19" — predicts ~4 proactive foundation patches (one per blocking row) when it lands.

  **Pattern (m) extension — integrated cold-read at higher theme density**: Row 18 ~24 themes integrated total (foundation 5 + post-commit 6 + cf-view doc-gap 2 + cf-view cold-read 3 + Saint-Venant inversion 12-as-1-finding + sub-micron 1 + end-of-session 4 ≈ 22-33 depending on whether Saint-Venant inversion is counted as 1 finding or 12 corrections). Row 14: 16 themes. Row 18 ~50% higher density driven by the inverted-Saint-Venant interpretation finding which spawned a 12-location simultaneous fix + cold-read iterations on the rewrite. Pattern (m) extension: when a single cold-read finding triggers a multi-location rewrite (inversion fix, naming change, etc.), the rewrite itself spawns a follow-up cold-read pass to catch drift in the rewrite — adds ~1 cold-read commit beyond the standard 4-pass cadence.

  **Banked at this iter (cross-arc-relevant patterns):**

  - **(o) Visual-review can catch INTERPRETATION-LEVEL bugs that no test catches.** Sister of pattern (h) iterative-visual-review doc-rot at the **semantic-correctness layer** (vs h's syntactic doc-staleness layer). The Saint-Venant pattern story I documented across ~12 locations was INVERTED everywhere (corner highest pressure → actually interior highest); only the discrete-marker plot.py PNG made it obvious. Numerics were correct throughout (tests pass); only the human prose framing of "which physical vertex corresponds to which extremum" was inverted. Trigger: any row with a per-vertex statistical readout where the "which vertex maps to which extremum" interpretation is documented in prose.

  - **(p) When an inversion-fix preserves surrounding wording while flipping a direction, the preserved wording itself may be wrong.** The "differs by a few μm" claim was in the original `11cea6f1` row 18 commit AND survived the inversion fix at `5640cdf0` that flipped corner ↔ interior — but actual variation is sub-micron (~0.25 μm), 10× smaller than claimed. Cold-read of `5640cdf0` caught it. **Bank**: during inversion fixes, verify all adjacent numerical claims with captured-bits arithmetic, not just the inverted direction-claim itself.

  - **(q) README inside ` ```text ` code-fenced blocks won't render `**bold**` markers.** Markdown spec: code-fenced content is literal, no markdown rendering. When adding prose to existing code-fenced sections, check whether the prose belongs INSIDE (literal) or OUTSIDE (rendered) the fence. Caught at the cf-view doc-gap commit's cold-read (`f66b347e` T2). Sister of pattern (n) `serde_json::json!` macro recursion-limit at the markdown-fence layer.

  - **(r) Discrete-marker visualization (no smoothing) is the load-bearing visual-review surface for spatial-distribution interpretation** — for rows shipping per-pair statistical readouts. Smooth-shaded mesh viz (cf-view PLY w/ smooth normals) DIFFUSES the per-pair gradient via vertex-scalar interpolation across the boundary triangulation; the discrete-marker scatter (plot.py per-pair) preserves the per-vertex values cleanly. **Bank**: for rows shipping per-pair readouts, plot.py PNG > cf-view PLY for verifying spatial-distribution claims. cf-view PLY surfaces the engagement boundary + mean level; plot.py surfaces the per-pair gradient.

  - **(s) End-of-session cold-read catches CROSS-FILE drift the in-commit cold-reads miss by construction.** In-commit cold-read looks at the local diff (single file or single commit's surface); cross-file claims that go stale when a parallel file was updated separately don't surface until the integrated end-of-session pass. Pattern (m) extension: the 4-pass cadence's 4th pass (end-of-session) is essential, not optional; for arcs with cross-file source surfaces (main.rs + README + plot.py), the end-of-session pass typically surfaces 2-5 additional themes — row 18 surfaced 4 (T1 parallel-claim drift, T2 stale-literal-quote, T3+T4 stale-mirror-claim).

  **PR2 commit count update (iter-13):** As of `2768a3ef` (end-of-session cold-read), **50 commits total** = 26 (foundation phase + row 12 per iter-11) + 8 (row 13 cluster per iter-12) + 4 (row 14) + 2 (iter-12 banking + amendment) + 10 (row 18 cluster). Final PR2 commit count vs iter-12 estimate of 40-46: **+4 over the upper bound** (50 vs 46). Overshoot driven by row 18's higher-than-expected cold-read overhead (24 themes vs row 14's 16, ~50% higher) — primarily from the Saint-Venant inversion fix spawning rewrite cold-reads. iter-13 banking will add 1 commit (this entry); MEMORY.md "Resume here" update will add 1 more; bringing the total to **52 commits** before PR2 ship-time decisions.

  **Pre-flight per row-18 commit: build, fmt --all, clippy --workspace -- -D warnings, cargo doc -D warnings, xtask grade A on automated criteria.** **Pushed to origin progressively** as iter-13 commits land; current `dev` tip is recorded in MEMORY.md `Resume here` header.

  **Next iteration entry point: PR2 ship-time decisions.** All four PR2 example rows shipped (rows 12 + 13 + 14 + 18); foundation phase complete; cf-view + sim-bevy-soft + cf-bevy-common foundations all stable. Decisions to lock: (1) squash-merge strategy — single PR per PR1 precedent (recommended) or per-row PRs? (2) pre-squash tag at `feature/sim-soft-pr2-pre-squash` per `feedback_pre_squash_tag` (recommended given the cf-bevy-common births + 4 example rows + ~24-theme cold-read history is worth preserving in tag history). (3) book-chapter updates? (Row 18's per-pair readout surface is a candidate for Part 4 §00 §00 amendment — narrows the "validation scope" framing to also include the user-facing readout API surface.) (4) Banked row-14 back-port followup (centered plates + ASCII HUD + sub-micron fix mirror; non-blocking; can ship before or after PR2 squash). PR3 (Tier 5/6 silicone-device path) remains BLOCKED on cf-design bridges per inventory.

- **2026-05-08 (iter 14 — row 15 mesh-scan-as-solid shipped + 0 reactive foundation patches; PR3 first row complete):** Row 15 shipped end-to-end across **3 commits** = `4ac04e79` (initial row commit, with pre-commit cold-read 6 themes folded in: stale grid resolution / divergence-zero / face-band scope / dot-tolerance wording / F2-F3 attribution / "fixture" prose) + `38318dbd` (post-commit N+1 cold-read 4 themes: bbox-corner SDF arithmetic re-derivation per banked pattern (g) + face-band probe accounting + stale grid-axis comment 0..21 vs 0..17 + PLY size estimate ~80 KB vs actual 96 KB) + `bebccc97` (visuals-pass N+2 driven by user's cf-view eyes-on-pixels review, 3 themes: launch-view colormap claim "sequential viridis" → categorical, HE-1 surface description "y=z diagonal of +X face" → y=z 2D plane through whole body, signed_distance "what you should see" framing missing the visual-scale gap between strict-interior 343-point cube and bbox 4913-point cloud). 13 themes total across 3 cold-read passes.

  **Locked design calls (row 15 in-arc):**

  1. **Programmatic 12-tri cube fixture, NOT checked-in STL asset** — mirrors `examples/mesh/mesh-sdf-distance-query`'s octahedron precedent. Self-contained, analytically tractable, sanitization-clean by construction (no scan data anywhere in repo per layered-silicone-device memo's directive). Closed-form L∞-ball SDF: `√(sum max(d_i, 0)²) + min(max(d_i), 0)` with `d_i = |p_i| − R` (textbook box SDF, Inigo Quilez).

  2. **Trait-dispatch via `&dyn cf_design::Sdf`, not inherent `SignedDistanceField` API** — the load-bearing F2 demonstration. Every closed-form anchor goes through the trait surface. Row 16 will exercise the parallel F3 path (sim_soft consumers via re-export); row 15 stays cf-design-side.

  3. **STL save→load round-trip in-example** — demonstrates the "scan-import workflow" without checked-in asset. STL stores binary f32 vertex coords, but integer `±R = ±1.0` round-trips losslessly through f32. Bit-exact agreement at named probes (face-region + origin) — STL's lossy face-indexing (8 → 36 vertices via no-dedup load) doesn't affect SDF computation since mesh-sdf consumes faces directly.

  4. **17³ × 0.25 dyadic bulk grid (NOT 21³ × 0.2)** — first draft used 21³ × 0.2 in `[−2, 2]³`; switched after `1.0 − 0.2 = 0.79999999999999998` and `5 × 0.2 = 1.0` produced 1-ULP slack at axis ±0.6/±0.8 that broke closed-form strict-interior count. Spacing 0.25 = 2⁻² is dyadic; every grid axis coord bit-exact in f64; ±R = ±1.0 lands exactly at axis indices 4 and 12.

  5. **Three load-bearing bulk-grid assertions + two empirical pins** — (a) closed-form-vs-trait identity at every grid point (1e-12); (b) heuristic strict-interior coverage (343 of 4913); (c) raycast off-diagonal strict-interior coverage (294 of 343, the y≠z subset; the 49 y=z probes hit HE-1 → no claim made); (d) F2-caveat-absent identity `heuristic_inside == STRICT_INTERIOR_COUNT` (no F2-caveat false-positives on cube); (e) empirical pins `raycast_inside == 576` + `divergence == 331` (drift detector for mesh-sdf parallel-/edge-ray policy).

  6. **HE-1 raycast degeneracy on cube y=z plane** — same phenomenon `mesh-sdf-distance-query` documents at the octahedron origin, surface differs (cube: 2D plane; octahedron: 0D vertex point). Cube's +X face has 2 triangles `[1,2,6]` + `[1,6,5]` sharing the y=z diagonal edge from vertex 1 to vertex 6; +X-direction ray from any probe at (any_x, c, c) hits this edge → Möller-Trumbore parallel-ray `None` → undercount → `is_inside` flips to false. 49 strict-interior probes affected. Documented in `verify_grid_consistency` filter with localized `#[allow(clippy::float_cmp)]` (dyadic grid coords make `y == z` bit-exact-meaningful).

  **Headline result**: trait dispatch works end-to-end; closed-form-vs-trait identity holds bit-exactly across 4913 bulk grid points within `BAND_TOL = 1e-12`; strict-interior heuristic coverage is 100 %; off-diagonal strict-interior raycast coverage is 100 % (294/294); HE-1 surface dimensionality empirically verified as 2D plane through the body (not 1D edge on a single face).

  **Banked at this iter (cross-arc-relevant patterns):**

  - **(t) HE-1 raycast degeneracy is a 2D surface, not a 1D edge** — for triangulated meshes with face-internal diagonals, +X-direction Möller-Trumbore parallel-ray epsilon fires on the full *plane* through the body intersecting that diagonal, not just the diagonal line itself. Surface dimensionality of the failure depends on triangulation topology — face-internal diagonals → 2D plane through body; vertex-degenerate corners (mesh-sdf-distance-query octahedron) → 0D point. Trigger: any consumer reasoning about ray-cast inside-test reliability on a triangulated mesh; assert 2D rather than 1D when the failure axis is a face-internal diagonal.

  - **(u) cf-view auto-detect colormap heuristic empirically validated** — closes iter-12's C2 line. cf-view's auto picks: binary 0/1 → categorical (blue/orange palette), straddles-zero continuous → divergent (red-blue), unipolar continuous → sequential (viridis). Three categories, three palettes. The iter-12 banked observation of "C2 disproven" was correct re: sequential for unipolar; iter-14 confirms by adding the binary/categorical and straddles-zero/divergent cases. README colormap claims for cf-view-emit rows should match the auto-pick (or document user-override); row 15's pre-N+2 README incorrectly claimed "sequential viridis" for binary `inside_raycast` (actual: categorical).

  - **(v) Visuals-pass-as-N+2 for static-PLY rows** — sister of pattern (e) iterative-visual-review collapse for Bevy rows, at the static-artifact layer. The user's cf-view review of a Tier-1/Tier-5 row's PLY surfaces (a) colormap-claim README drift, (b) spatial-feature description drift (e.g. "y=z diagonal of +X face" → actually y=z 2D plane through whole body), (c) visual-scale framing gaps (e.g. "small interior" framing missing when bbox is much larger than the cube). Cadence for static-PLY rows: 1 row + 1 N+1 (pre- or post-commit cold-read) + 1 N+2 (visuals-pass-driven) = 3 commits per row when visuals surface drift; 2 commits if visuals match prediction perfectly. Row 15: 3 commits (visuals surfaced 3 themes).

  - **(w) Foundation-maturity correlates inversely with reactive-patch count** — pattern (j) refinement. Reactive foundation-patch count for a first-consumer row scales inversely with foundation completeness at row-author time, not just first-consumer status. Row 12 (sim-bevy-soft <30 days post-birth, 3 modules unborn): 3 reactive. Row 13 (cf-view ~1 week post-RENDER_SCALE pivot): 2 reactive. Row 14 (sim-bevy-soft mature): 0 reactive. Row 15 (F1+F2 contract-tested, mesh-io 1.0): 0 reactive. Predict row 16's reactive count as 0–1 (cf-design `Solid` + `SdfMeshedTetMesh::from_sdf` are mature; only `Solid::from_sdf` ctor in question if heterogeneous-CSG forces it). Row 20's reactive count: high (heterogeneous-CSG architectural decision must surface; predict 1–3 cf-design foundation patches).

  **PR3 commit count update (iter-14):** As of `bebccc97`, **13 commits total** = 10 (foundation phase) + 3 (row 15 cluster: row + N+1 + N+2). iter-14 banking will add 1 commit (this entry); MEMORY.md "Resume here" update was applied separately (local file, no commit). Bringing the total to **14 commits** before row 16 work begins. PR3 final estimate (post-iter-14): ~25–35 commits — substantially lighter than PR2's 52, driven by the foundation-maturity pattern (w) (cf-design pre-mature; rows 16/19 expected to consume mature surface with 0–1 reactive patches each). Row 20 may add architectural overhead.

  **Pre-flight per row-15 commit: build, fmt --all, clippy --workspace -- -D warnings, cargo doc -D warnings, xtask grade A on automated criteria.** **Pushed to origin progressively** — current `dev` tip at `bebccc97` after this iter-14 banking + push.

  **Next iteration entry point: row 16 `solid-to-sim-soft`.** First sim_soft-side bridge consumer — exercises F3 (sim_soft's `Sdf` re-export from cf_design) by composing `Solid::sphere(R)` boolean-difference `Solid::sphere(r)` and meshing via `SdfMeshedTetMesh::from_sdf`. Architectural-pivot heads-up flag: row 16's spec asks for `SdfMeshedTetMesh::from_sdf(&solid, ...)` over a `Solid` operand — verify whether existing `from_sdf` already accepts a `&Solid` (likely yes since cf_design::Sdf is now the trait surface; if not, foundation patch). Predicted reactive foundation-patch count: 0–1 per pattern (w). Recon for row 16: read this file's row 16 spec (line 84) + row 15 as immediate precedent + cf_design's `Solid::sphere`/`subtract` API + sim_soft's `SdfMeshedTetMesh::from_sdf` signature. New session recommended for phase transition; row 15's HE-1 + cf-view-auto-detect + visuals-pass-as-N+2 patterns transfer to row 16 as banked context.

- **2026-05-08 (iter 15 — row 16 solid-to-sim-soft shipped + 0 reactive foundation patches; bridge story closed):** Row 16 shipped in **5 commits** (initial row commit `82aaca4b` + N+1 prose fixup `62c35ace` + N+2 predict-then-verify fixup `9dfec253` + N+3 eyes-on-pixels fixup with cf-view artifact pivot `376d9ce4` + N+4 cold-read fixup on N+3's prose; pattern-(x) prediction "1-commit row" was REFUTED across 4 progressive cold-read passes — N+1 surfaced narrative-prose drift, N+2 (PLY-binary inspection) surfaced colormap-range drift introduced BY N+1, N+3 (user's actual cf-view eyes-on-pixels review) surfaced VISUALIZATION-FAILURE drift the predict-then-verify N+2 missed AND triggered an artifact pivot, N+4 (cold-read on N+3's prose) surfaced numeric-value drift introduced BY N+3 when carrying boundary-vertex numbers forward into the centroid-based artifact framing). Recon predicted 0 reactive foundation patches per pattern (w); landed exactly at 0 — `SdfMeshedTetMesh::from_sdf(sdf: &dyn Sdf, ...)` accepts `&Solid` directly via DST coercion since F1 ships `impl Sdf for Solid` and F3 re-exports `cf_design::Sdf` as `sim_soft::Sdf`. The bridge worked first-try. PR3 row count: 4 → 2 shipped (rows 15, 16); 2 to go (rows 19, 20).

  **Locked design calls (row 16 in-arc):**

  1. **Same radii as row 11** — `LAYERED_SPHERE_R_OUTER = 0.10`, `LAYERED_SPHERE_R_CAVITY = 0.04` re-used from sim-soft re-exports. Mesh topology = f(geometry, cell_size); single-material variant produces identical tet/vertex/boundary counts to row 11's three-shell scene. Cross-row continuity: every count + the cavity-wall mean bits match row 11's uniform-1× capture exactly.

  2. **`material_field: None` (skeleton_default)** — single-material variant of row 11. `MaterialField::skeleton_default = uniform(1e5, 4e5)` is bit-equal to row 11's `(MU_INNER, LAMBDA_INNER)`, so the FEM observed cavity-wall mean is bit-equal to row 11's `CAVITY_WALL_UNIFORM_1X_REF_BITS = 0x3f35_43f1_0b56_98c8 ≈ 3.244842e-4 m`. Pinned as IV-1 sparse-tier rel-tol guard (`assert_relative_eq!` at `1e-12`).

  3. **HEADLINE A — bridge equivalence anchor at `EXACT_TOL = 0.0`.** Mesh the body via two SDF surfaces (typed `Solid` AND sim-soft's PR1-era `DifferenceSdf<SphereSdf>`); assert `equals_structurally` + position-by-position bit-equality. cf-design's `Sphere` evaluates `p.coords.norm() - radius` (verified at `design/cf-design/src/evaluate.rs:216`) and `Subtract` is `a.max(-b)` (line 59); sim-soft's `SphereSdf` and `DifferenceSdf` use identical arithmetic. The BCC + warp + stuffing pipeline samples bit-equal SDF values and produces bit-equal meshes — semantic-equivalence anchor at the strongest possible tolerance.

  4. **HEADLINE B — single-material Lamé.** Closed-form derivation in module docstring + verified hand-calculation in plan: `u_r(R_a) ≈ 3.823e-4 m` analytic; FEM observed `≈ 3.245e-4 m` at h/2; rel-err `~15 %` under the row 11 `RADIAL_REL_TOL = 0.30` cavity-gate signature. Row 11's three-shell 6×6 LU collapses to 2×2 in single-material — no inline LU solve needed; `cavity_wall_lame_analytic(μ, λ, p, R_a, R_b)` is one closed-form expression.

  5. **No N+2 visuals-pass scheduled in the row commit** — the deformed-boundary PLY artifact carries a single `radial_displacement` scalar (sequential viridis on `[0, ~5e-4]`); cf-view's auto-pick is the only scalar present. Per pattern (v), visuals-pass-as-N+2 is conditional on visual drift surfacing during user review; the row 16 commit ships without it pending user's eyes-on-pixels review. Predict 0-1 themes if review surfaces drift; 0 otherwise.

  6. **`SoftScene::layered_silicone_sphere` constructor NOT used** — that constructor builds via `DifferenceSdf<SphereSdf>` internally, defeating the bridge demonstration. Row 16 builds the BC inline (cavity-wall band + outer-wall band via `pick_vertices_by_predicate` + half-cell `band_tol`) mirroring `layered_silicone_sphere`'s implementation verbatim but typed-`Solid`-driven for the meshing.

  **Headline result**: 0 reactive foundation patches; bridge worked first-try. n_tets=6456, n_vertices=4682, n_referenced=1480 (all bit-equal to row 11). Newton converges in 3 iters at residual `~3.4e-12`. Cavity-wall observed `3.244842e-4 m` (bit-equal to row 11's uniform-1×); rel-err vs single-material analytic `3.823529e-4 m` is `15.13 %`.

  **Banked at this iter (cross-arc-relevant patterns):**

  - **(x) Recon-driven prediction-fit reduces N+1 finding density on QUANTITATIVE anchors but does NOT eliminate the pass.** Original framing ("eliminates the N+1 pass") was REFUTED at the row 16 N+1 cold-read (user-requested), which surfaced 3 themes despite all quantitative anchors prediction-fitting bit-exactly: (1) cf-view artifact framing in module docstring AND README conflated analytic prediction `~3.8e-4 m` with FEM observed `~3.2e-4 m` (the PLY emits observed, not analytic — pattern (g)-flavored conflation at the user-facing visual layer); (2) `save_boundary_ply` docstring claimed "row 11's z-slab cf-view artifact" precedent but row 16's full-boundary-surface artifact follows row 3's `sphere-boundary.ply` pattern (DISPLACEMENT_SCALE = 50.0 IS row 11's value at the constant level, but artifact SHAPE is row 3's — pattern (e)-flavored stale-precedent claim); (3) `verify_bridge_equivalence` carried a dead `let _ = v;` + 4-line comment about "future diagnostic ergonomics" — premature future-proofing per CLAUDE.md no-comments-unless-WHY-non-obvious + no-premature-abstractions rules. **Refined banking**: recon-driven prediction-fit eliminates *quantitative-anchor* drift on N+1 (counts, observed-vs-captured bits, rel-err prediction-fit bit-exactly to recon); does NOT eliminate *narrative-prose* drift (analytic-vs-observed conflation, stale-precedent cross-row claims, code-quality leftovers from authoring). Cadence variant: **3-commit row** (initial + N+1 + N+2) when recon prediction-fits cleanly AND visuals-pass surfaces drift; quantitative anchors are bit-exact at row commit, narrative drift surfaces at N+1, visual-grounded narrative drift surfaces at N+2. Refines pattern (m): N+1 cold-read is ALWAYS warranted; what changes with recon-driven prediction-fit is the *theme density* (~3 themes vs row 15's 13 themes), not the existence of the pass. Pattern (h)+(v) iterative-visual-review and visuals-pass-as-N+2 still apply post-user-review independently.

  - **(z) Cold-read passes are NOT idempotent — every prose rewrite can introduce new drift the next cold-read catches; convergence often takes ≥ 2 cold-read passes after each substantive rewrite.** Originally framed at row 16 N+2 as "N+1 cold-reads can introduce NEW drift that visuals-pass N+2 catches"; refined at row 16 N+4 (cold-read on N+3 pivot) to the more general principle. Specific instances on row 16:
    - **N+1 → N+2 instance:** N+1 corrected "`[0, ~5e-4]`" → "`[0, ~3.5e-4]`" but used the HEADLINE B mean as bound proxy when peak was needed; N+2 PLY-binary inspection caught the upper-bound undershoot (correct: `[0, ~4.3e-4]` for boundary-vertex peak).
    - **N+3 → N+4 instance:** N+3 pivoted the artifact from boundary-surface to z-slab centroid cloud and rewrote the prose around the new artifact, but carried forward boundary-vertex numbers (peak `~3.4e-4 m`, range `[0, ~3.4e-4]`) into the centroid framing without re-deriving for centroids; N+4 PLY-binary inspection on `shell_zslab.ply` showed centroid peak is `~3.005e-4 m`, ~12 % smaller than the boundary-vertex mean, because centroid displacement averages 4 vertex displacements. N+4 also caught a ring-radius framing drift ("`|p_xy| ≈ R_CAVITY = 0.04 m`" was 33 % off from the actual `~0.054 m` cavity-side centroid radius, since centroids are interior to the wall by half-cell-edge by construction).
    **Refined banking**: pattern (m) "N+1 cold-read" alone is insufficient for cf-view-emit rows; pattern (v) visuals-pass-as-N+2 (PLY-binary inspection) is necessary-but-insufficient when artifact PIVOTS happen at N+3; cold-read on N+3 prose (= N+4) catches numeric-carry-forward drift when the new artifact has different statistics. **General principle**: every cold-read pass that REWRITES PROSE risks introducing new drift; budget ≥ 1 additional cold-read pass after each substantive prose rewrite. The cold-read sequence isn't idempotent — empirical convergence rate on row 16 was ~1 finding per pass over 4 passes (3 + 3 + 3 + 5 themes = 14 themes total), with each pass consuming 80-90 % of prior-pass findings but introducing ~3 new ones via prose-rewrite-during-fixup.

  - **(aa) Predict-then-verify N+2 is necessary-but-insufficient: actual cf-view eyes-on-pixels review IS the load-bearing visuals pass.** Row 16 N+2 (`9dfec253`) used PLY-binary inspection to verify scalar range, vertex-set cardinality, bbox — every prediction was numerically accurate. The N+2 banking framing claimed "the field is bimodal at the boundary surface layer (outer at 0, inner at 2.6-4.3e-4)" with a "rotate to see the inflated cavity" hint. **Refuted at row 16 N+3** when the user actually launched cf-view: across all 3 colormap modes (divergent / sequential / categorical), the artifact rendered as a SINGLE UNIFORM-COLORED OUTER SPHERE — outer shell occluding inner cavity 360° from every orbit angle, cf-view exposing no section-cut UI. The "rotate to see" hint was wrong; rotation only shows the same outer sphere from a different angle. The artifact was VISUALLY USELESS as the boundary-surface PLY for a hollow body. Root cause: the row-3 boundary-surface precedent works for SOLID bodies (where the boundary surface IS the displaced/interesting region) but fails for HOLLOW bodies (where the interesting region — the cavity wall — is the OCCLUDED inner surface). The N+3 fix pivots to row 11's z-slab per-tet centroid pattern, which exposes both inner + outer rings simultaneously as a 2-D annular projection through the body's middle. **Refined banking**: predict-then-verify (PLY inspection) catches QUANTITATIVE PLY drift (scalar range, count, distribution) but cannot predict OCCLUSION FAILURES that depend on viewer-UI semantics; only the actual eyes-on-pixels cf-view review surfaces those. For cf-view-emit rows on hollow / occluding-geometry bodies, default to row 11's z-slab pattern from the start; the row-3 boundary-surface pattern is a SOLID-body-only precedent. New cf-view artifact-shape decision rule: if the body has interior cavities or partial-occlusion geometry, use z-slab centroid clouds; if the body is solid + the boundary IS the interesting surface, use row-3's full-boundary-surface mesh. Cadence floor revised: cf-view-emit rows on hollow bodies need at least N+0 (initial) + N+1 (cold-read) + N+2 (PLY-binary verify) + N+3 (eyes-on-pixels) = **4-commit floor** when artifact-pattern needs pivoting; 3-commit floor when artifact pattern is right from the start.

  - **(y) Bit-equal cross-row continuity captures regressions across rows.** Row 16's `CAVITY_WALL_MEAN_REF_BITS = 0x3f35_43f1_0b56_98c8` is the bit-pin of row 11's uniform-1× cavity-wall mean. Same geometry, same Lamé pair, same solver path ⇒ bit-equal observation regardless of which row's main runs. **Sister of pattern (j) foundation-maturity correlation, at the cross-row-readout layer**: when two rows exercise the same FEM hot path with overlapping inputs, capturing the same readout bits in both rows is a load-bearing regression net — a regression in the shared hot path fires in BOTH rows simultaneously. Trigger: any row whose readout overlaps an earlier row's; pin the bits in both at the same captured value with rel-tol guard.

  **PR3 commit count update (iter-15, post-N+4):** As of N+4 cold-read fixup commit (cold-read of N+3's prose pivot + pattern (z) refinement), **19 commits total** = 14 (foundation + row 15 + iter-14 banking) + 1 (row 16 initial `82aaca4b`) + 1 (N+1 prose fixup `62c35ace`) + 1 (N+2 predict-then-verify `9dfec253`) + 1 (N+3 eyes-on-pixels artifact pivot `376d9ce4`) + 1 (N+4 cold-read on N+3's prose, including iter-15 amendments + this paragraph + pattern (z) refinement). PR3 final estimate (post-iter-15-amended): ~27–35 commits — refined further from prior estimates, driven by row 16 landing at 5 commits (vs row 15's 3 commits). Row 16 cold-read sequence empirically converged at N+4: 3 themes / N+1 + 3 themes / N+2 + 3 themes / N+3 + 5 themes / N+4 = 14 themes over 4 cold-read passes. Row 19 prediction: ~3–4 commits (silicone_table foundation-only doc-table demo, but pattern (z) suggests budget ≥ 1 cold-read after each prose rewrite — minimum row + N+1 + N+2 floor for any cf-view-emit row, plus N+3 if visuals surface drift). Row 20: ~6–12 commits given the heterogeneous-CSG architectural decision must surface AND the silicone-device geometry will likely trigger pattern (aa) z-slab considerations AND pattern (z) suggests ≥ 1 N+4-style cold-read on each prose pivot; pattern (w) predicts 1–3 reactive cf-design foundation patches.

  **Pre-flight per row-16 commit: build, fmt --all, clippy --workspace -- -D warnings (-- -D warnings via xtask grade clippy criterion), cargo doc, xtask grade A on automated criteria.** **Pushed to origin progressively** as iter-15 commits land; current `dev` tip recorded in MEMORY.md `Resume here` header.

  **Next iteration entry point: row 19 `silicone-material-table`.** First user-facing consumer of F4's `silicone_table.rs` const module. Predicted reactive count: 0 (F4 fully shipped + contract-tested). Concept: shipped doc table + code constants module mapping `{Ecoflex 00-10/20/30/50, Dragon Skin 10A/20A/30A}` → `(μ, λ, ρ)` w/ source citation. Concrete shape TBD — likely a side-by-side material comparison demo showing μ/λ/ρ ordering or hardness-sweep + a small NH energy comparison at fixed F probe across all 7 entries. New session NOT required (cf-design + sim-soft surface unchanged); row 19's design call mostly about narrative ("table demo" vs "hardness sweep" vs "stress-strain comparison").

- **2026-05-08 (iter 16 — row 19 silicone-material-table shipped + 0 reactive foundation patches; pattern (w) prediction holds):** Row 19 shipped in **3 commits** (initial row commit `8243065c` + N+1 cold-read fixup `67590c10` + N+2 cold-read-on-N+1 fixup with iter-16 amendments folded in; pattern-(w) prediction "0 reactive patches" landed bit-exact — F4's `pub const` consts + `to_neo_hookean()` const-fn bridge + the `Material` trait surface were all stable + contract-tested at row-author time). Pattern-(z) cadence held: pattern `feedback_visual_pass_collapses_for_json_rows` collapses the N+2 visuals-pass into nothing for material-reference data, so the visuals floor is row + N+1; but the N+1 cold-read introduced parallel-source-of-truth drift (T-COLD-1) that an N+2 cold-read-on-N+1 surfaced, lifting the prose floor to row + N+1 + N+2 = 3 commits. N+1 cold-read surfaced 4 themes (T1: README JSON schema field-order drift, semantic vs alphabetical actual; T2: API usage snippet showed F=I where outputs are all 0, replaced with F = diag(2, 1, 1) per row's actual probe; T3: "validates the production material library against itself" weak phrasing tightened to name what's actually being checked; T4: stdout `σ_100 small-strain (3μ) = data-sheet linearization` ambiguous `=` reframed). N+2 cold-read-on-N+1 surfaced 3 themes (T-COLD-1: `save_json` docstring schema in main.rs went stale because N+1 fixed the README parallel surface only — pattern (z) parallel-source-of-truth amendment, see (bb) below; T-COLD-6: stream-of-consciousness "Wait that's 21. Let me recount:" arithmetic-correction prose left in this iter-16 entry — pattern (g) bottom-up-arithmetic violation at iter-16 banking time; T-COLD-7: `~~150` markdown-strikethrough typo, intent was `~150` for "approximately"). Recon-driven prediction-fit empirically held — all quantitative anchors prediction-fit bit-exactly (7 materials × 3 verify groups all green at first-run capture); themes were narrative-prose drift only, consistent with pattern (x).

  **Locked design calls (row 19 in-arc):**

  1. **Probe at simple stretch F = diag(2.0, 1, 1), NOT traction-free uniaxial** — `λ = 2.0` mirrors the Smooth-On `σ_100 = 100 % engineering strain` data-sheet anchor; simple stretch (transverse stretches pinned at 1, J = λ = 2) carries volumetric strain not what a tensile-test apparatus measures, but the closed form is scalar arithmetic with no inner Newton, which is what makes the multi-material gate clean. Row 5 already covers traction-free uniaxial across the in-domain bracket on one material; row 19's value is the cross-material comparison at one fixed `F`, not a redundant traction-free sweep.

  2. **JSON + stdout, NOT cf-view PLY or matplotlib plot** — per `feedback_visual_pass_collapses_for_json_rows` material-reference data has no native cf-view artifact; the table IS the visual. Stdout museum-plaque + JSON trace covers both human-readable and programmatic-consumption use cases at zero foundation cost; matplotlib stress-strain sweep across `λ ∈ [1.0, 1.5]` was considered but adds row-5-redundant content. Row 5 + row 19 split: row 5 is the curve-shape story per material, row 19 is the cross-material comparison at one anchor.

  3. **σ_100 PSI → Pa conversion in-row, NOT a sim-soft helper** — `PSI_TO_PA = 6_894.757_293_168_361` lives in `src/main.rs` as a row-local constant rather than `sim_soft::material::silicone_table::PSI_TO_PA`. The conversion is engineering-domain trivial; promoting to a public const is API-surface-bloat for one consumer (this row). Row 19 prints both PSI and Pa columns, JSON includes both per-material `sigma_100_psi` and `sigma_100_pa` fields + a top-level `psi_to_pa` factor for round-trip.

  4. **Captured-bits self-pin per material at IV-1 dense bit-equal tier** — pure-analytic probe, no sparse-solver path → `to_bits()` strict equality applies (NOT IV-1 sparse-tier rel-tol that admits faer's per-column FMA-fusion drift). 21 pins (3 fields × 7 materials). Cross-row continuity bit-pin to rows 11/16 was considered but rejected: none of the 7 F4 entries match `MaterialField::skeleton_default = uniform(1e5, 4e5)` bit-equally (ECOFLEX_00_30 has `(μ, λ) = (2.3e4, 9.2e4)` — close framing but different magnitudes), so pattern (y) cross-row bit-pin doesn't cleanly extend here. Self-pin within row 19's own captures is the regression net.

  5. **CF_CAPTURE_BITS=1 env-var bootstrap + re-bake helper** — first-run problem (verify_captured_bits fails before the print helper runs) solved by `CF_CAPTURE_BITS=1` env-var: when set, skip the verify and print the `CAPTURED_BITS` block ready to paste into source. Used at land-time + on intentional re-bake (e.g. F4 const value updated to a new data-sheet revision). IV-1 protocol forbids re-bake to silence a drift assertion.

  6. **N+2 cold-read-on-N+1 needed because N+1 fixed only the README parallel surface** — pattern (m) cadence: commit + 1 N+1 + 1 N+2 = 3 commits floor for JSON-only rows when parallel sources of truth exist (here: schema example in README AND `save_json` doc-comment in main.rs). Pattern `feedback_visual_pass_collapses_for_json_rows` collapses the visuals-pass N+2 into nothing for material-reference data; the prose-rewrite-budget N+2 (per pattern (z)) is what surfaced T-COLD-1 / T-COLD-6 / T-COLD-7. Row 15 was 3 commits (row + N+1 cold-read + N+2 visuals-pass); row 19 = 3 commits with the same shape but different N+2 trigger (parallel-source-of-truth drift, not visuals).

  **Headline result**: 0 reactive foundation patches; recon predicted 0 (per pattern (w)); landed at 0. All 7 anchor groups green at first-run capture. JSON 9-field schema per material × 7 materials emitted.

  **Banked at this iter (cross-arc-relevant patterns):**

  - **(bb) JSON-only rows have a 3-commit floor under recon-driven prediction-fit when parallel-source-of-truth surfaces exist** — pattern (m) "N+1 cold-read is always warranted" × pattern `feedback_visual_pass_collapses_for_json_rows` "no visuals-pass for JSON-only" × pattern (x) "recon prediction-fit reduces but doesn't eliminate N+1 finding density" → JSON-only rows with mature foundation land at row + N+1 + N+2 = **3 commits** when N+1 prose-rewrite introduces new drift in a parallel source surface. Originally banked as 2-commit floor at iter-16 N+1; refuted at iter-16 N+2 cold-read which surfaced T-COLD-1 (`save_json` docstring schema in main.rs went stale because N+1 fixed only the README schema example, not the parallel doc-comment example in source) + T-COLD-6 (stream-of-consciousness arithmetic-correction prose left in this very iter-16 banking entry: "Wait that's 21. Let me recount:" — pattern (g) bottom-up-arithmetic violation at authoring time) + T-COLD-7 (`~~150` markdown-strikethrough typo, intent was `~150` for "approximately"). Empirically: row 19 = 3 commits (4 N+1 themes + 3 N+2 themes). Trigger: any new sim-soft example with mature foundation + JSON-only artifact; expect 3 commits unless (a) F4-equivalent foundation is itself <1 day old (pattern (w) reactive patches surface), (b) the row introduces novel architectural surface (heterogeneous-CSG at row 20), or (c) the prose burden exceeds ~150 lines docstring + ~150 lines README (more prose surface = more drift potential). 2-commit floor only achievable when ALL claims live in one source surface AND N+1 cold-read covers the full surface bit-by-bit.

  - **(cc) Captured-bits CF_CAPTURE_BITS env-var bootstrap pattern** — pure-analytic rows that self-pin captured bits face a chicken-and-egg problem: the verify_captured_bits assertion fails before the print-helper can emit the bits-to-paste block. Solution: env-var-gated branch in main, when set skip verify + print the block, else verify. Single-source authoring + re-bake on intentional code edit (F4 const value updated to new data-sheet revision) without copy-pasting hex constants from terminal output. Trigger: any pure-analytic row with N captured bits to self-pin where the bits aren't trivially derivable in source.

  **PR3 commit count update (iter-16, post-N+2):** **22 commits total** = 19 (foundation + row 15 + row 16 cluster per iter-15) + 3 (row 19 cluster: initial `8243065c` + N+1 cold-read fixup `67590c10` + N+2 cold-read-on-N+1 fixup with this iter-16 amendment folded in). Breakdown: 14 (foundation + row 15 + iter-14 banking) + 5 (row 16 cluster: initial + N+1 + N+2 + N+3 + N+4 with iter-15 banking folded in) + 3 (row 19 cluster) = 22 commits. PR3 final estimate (post-iter-16-amended): ~25–32 commits — narrowed from iter-15's ~27–35 by row 19 landing at 3 commits (vs the iter-15 prediction of 3-4 commits — held at the upper end after N+2 surfaced T-COLD-1 / T-COLD-6 / T-COLD-7); row 20 remaining at ~6–10 commits given the heterogeneous-CSG architectural decision still must surface. Row 19 cold-read sequence empirically converged at N+2: 4 themes / N+1 + 3 themes / N+2 = 7 themes total over 2 passes (vs row 16's 14 themes over 4 passes — pattern (z) prose-rewrite-budget held; cold-read on N+1 surfaced parallel-source-of-truth drift the N+1 pass missed in main.rs while fixing only the README).

  **Pre-flight per row-19 commit: build, fmt --all (with pre-commit hook re-formatting tuple literals to multi-line), clippy --workspace -- -D warnings, cargo doc -D warnings, xtask grade A on automated criteria.** **Pushed to origin progressively** as iter-16 commits land; current `dev` tip recorded in MEMORY.md `Resume here` header.

  **Next iteration entry point: row 20 `layered-silicone-device`.** PR3's final row + the Tier 6 synthesis row that closes the entire sim-soft examples arc. **Architectural-pivot heads-up flag (load-bearing):** heterogeneous-CSG between typed `cf_design::Solid` operands and scan-derived `mesh_sdf::SignedDistanceField` may force a `Solid::from_sdf(impl Sdf, bounds)` ctor in cf-design (potentially via existing `FieldNode::UserFn`), OR keep sim-soft's `DifferenceSdf` (heterogeneous `Box<dyn Sdf>`) as escape hatch, OR a hybrid. Decision is row 20's call at author-time when scene shape forces the answer. Pattern (w) predicts 1–3 reactive cf-design foundation patches; pattern (aa) z-slab artifact predicted for hollow silicone-device geometry (cf-view artifact-shape decision rule: hollow → z-slab, solid → boundary-surface); pattern (z) suggests budget for ≥ 1 N+4-style cold-read on each prose pivot. **SphereSdf grad-singularity divergence cleanup** between sim-soft's `Vec3::z()` and cf-design's `Vector3::zeros()` at the origin is a separate cleanup commit, not folded into row 20. **Sanitization**: scan reference geometry only, no anatomical references anywhere visible. **Material budget**: 2 lb / silicone hard cap. Recon for row 20: read this file's row 20 spec (line 93) + row 16 as immediate precedent + cf_design `Solid::sphere` / `subtract` / `from_sdf` candidates + sim_soft `MaterialField::layered` + `PenaltyRigidContact::new<S: Sdf>` per PR2's trait unification + scan-mesh → `mesh_sdf::SignedDistanceField` → `cf_design::Sdf` flow (row 15 precedent). New session likely warranted given the architectural-decision surface.

---

## Cross-references

- `project_layered_silicone_device` (memory) — anchoring engineering goal.
- `project_gameplan` (memory) — six-phase soft-body plan; Phase 5 shipped 2026-04-29.
- `project_phase_5_commit_log` (memory) — Phase 5 deliverables (`RigidPlane`, `PenaltyRigidContact`, `RigidPrimitive` trait).
- `feedback_examples_drive_gap_fixes` (memory) — examples are *just as much* for fixing gaps as for showcasing.
- `feedback_simplify_examples` (memory) — realistic-looking parts before combining domains.
- `feedback_one_at_a_time` (memory) + `feedback_one_at_a_time_review` — review examples individually with two-pass review.
- `feedback_museum_plaque_readmes` (memory) — one concept per visual.
- `feedback_pr_size_ci_economics` (memory) — internal commit segmentation, not PR splitting.
- `feedback_code_speaks` (memory) — delete this file after the cleanup arc completes.

## Resume-here for next session

1. Read this file end-to-end.
2. Read `project_layered_silicone_device` memory memo.
3. Confirm scope + open questions §1-§5 are still right; redirect if not.
4. Begin authoring inventory rows. Start with capabilities you're sure of; mark uncertain ones with `?` for follow-up.
5. Update Iteration log with what you did + what's still open.
