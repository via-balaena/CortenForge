[package]
name = "mesh-shell-benches"
description = "Criterion benchmarks for mesh-shell (offset shell generation)"
publish = false
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true

[package.metadata.cortenforge]
tier = "L1"

[dependencies]
# Criterion harness for shell_benchmarks
criterion = { workspace = true }
# Crate under bench: ShellBuilder, ShellParams, generate_shell
mesh-shell = { workspace = true }
# IndexedMesh + Point3 inputs to shell generation
mesh-types = { workspace = true }

[[bench]]
name = "shell_benchmarks"
harness = false

[lints]
workspace = true
