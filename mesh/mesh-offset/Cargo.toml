[package]
name = "mesh-offset"
version = "0.7.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Mesh offset operations using signed distance fields"
repository = "https://github.com/cortenforge/cortenforge"
keywords = ["mesh", "offset", "shell", "3d", "geometry"]
categories = ["algorithms", "graphics", "game-development"]

[dependencies]
# Shared mesh data structures (workspace)
mesh-types = { workspace = true }
# Mesh signed-distance fields (workspace)
mesh-sdf = { workspace = true }
# Linear algebra (vectors, matrices, rotations)
nalgebra = { workspace = true }
# Derive error types
thiserror = { workspace = true }

[dev-dependencies]
# Approximate equality for numeric tests
approx = { workspace = true }
