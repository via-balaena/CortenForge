[package]
name = "mesh-region"
version = "0.7.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Region selection, material zones, and thickness mapping for triangle meshes"
repository = "https://github.com/cortenforge/cortenforge"
keywords = ["mesh", "region", "selection", "material", "3d"]
categories = ["algorithms", "graphics", "game-development"]

[dependencies]
mesh-types = { workspace = true }
nalgebra = { workspace = true }
thiserror = { workspace = true }
hashbrown = { workspace = true }

[dev-dependencies]
approx = { workspace = true }
