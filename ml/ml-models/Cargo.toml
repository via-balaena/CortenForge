[package]
name = "ml-models"
description = "Burn ML model architectures and checkpoint persistence for CortenForge"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
# Internal
ml-types = { workspace = true }

# Burn ML framework
burn = { workspace = true }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }

# Error handling
thiserror = { workspace = true }

[dev-dependencies]
burn-ndarray = { workspace = true }
tempfile = { workspace = true }

[features]
default = []
# Enable WGPU backend for GPU inference
wgpu = ["burn/wgpu"]
