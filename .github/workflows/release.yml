# CortenForge Release Workflow
#
# Triggered on version tags (v*.*.*)
# Creates GitHub releases with:
# - SBOM (Software Bill of Materials)
# - Auto-generated changelog from conventional commits
#
# ⚠ No binaries are attached. Cendrillon was shipped here as a 4-target
# download until 2026-09-02; it is now run from source, so the build/package/
# sign job and the app archives are gone. Restoring a download means restoring
# THREE things deleted with it: site/cendrillon/index.html's buttons (they 404
# without this job), site/style.css's `.downloads`/`.button*`/`details.firstrun`
# rules (or the buttons render unstyled), and tools/cf-studio-gui/macos/
# Info.plist (the .app packaging sed-substituted __VERSION__ from it). All
# three went in one commit, so `git show` on it is the restore list.
#
# See docs/INFRASTRUCTURE.md for the full constraint specification.

name: Release

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
      - 'v[0-9]+.[0-9]+.[0-9]+-*'  # Pre-releases like v0.8.0-rc.1

permissions:
  contents: write
  # ⚠ `id-token: write` was dropped 2026-09-02. It was granted "for SLSA
  # provenance", but no step ever minted or consumed an OIDC token, and the
  # only artifact signing that existed (ad-hoc `codesign` on the macOS .app)
  # went with the retired download. Re-add it in the change that actually
  # generates provenance — it is an elevated identity grant.

env:
  CARGO_TERM_COLOR: always

jobs:
  # ⚠ NOTHING HERE COMPILES THE TAGGED TREE. The deleted build job's
  # Apple-Silicon leg was `beta: false`, so a tag that did not build failed the
  # job and no release was published. The premise that made that redundant is
  # still the real one — correctness is gated on `main` by quality-gate.yml, and
  # a tag is only ever cut from already-green `main` — but it is now a CONVENTION
  # with nothing enforcing it. A tag pushed off a side branch, or cut from a
  # commit whose Quality Gate later went red, will publish a release whose only
  # instruction is "build it from source" for a tree that does not build.
  # ============================================================================
  # GENERATE SBOM
  # ============================================================================
  sbom:
    name: Generate SBOM
    runs-on: ubuntu-latest
    # ⚠ Was "supplementary — must never block the actual downloads". There are
    # no downloads since 2026-09-02, so the SBOM is now this release's ONLY
    # asset and a failure here publishes a release with zero files while the
    # body says one is attached. Real gap, deliberately NOT fixed here:
    # hardening it touches four separate tolerance points, an unpinned tool and
    # a filename nothing pre-merge exercises. Filed as #866, which carries the
    # four tolerance points and the measured cargo-cyclonedx naming probe.
    continue-on-error: true
    steps:
      - uses: actions/checkout@v7

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install cargo-cyclonedx
        run: cargo install cargo-cyclonedx

      # Current cargo-cyclonedx has no --output-file; it writes a
      # <crate>.cdx.json per package. Generate, then gather them.
      - name: Generate SBOM (CycloneDX)
        run: cargo cyclonedx --format json

      # cargo-cyclonedx writes one <crate>.cdx.json per workspace member
      # (~280 of them). Keep only Cendrillon's — attaching all of them as
      # release assets is noise AND trips GitHub's upload rate limit. ⚠ Not
      # "the shipped app" any more: nothing is shipped.
      - name: Collect Studio SBOM
        run: |
          mkdir -p sbom-out
          find . -name 'cf-studio-gui.cdx.json' -not -path './sbom-out/*' -exec cp {} sbom-out/ \;

      - name: Upload SBOM artifacts
        uses: actions/upload-artifact@v7
        with:
          name: sbom
          path: sbom-out/
          if-no-files-found: warn

  # ============================================================================
  # GENERATE CHANGELOG
  # ============================================================================
  changelog:
    name: Generate Changelog
    runs-on: ubuntu-latest
    outputs:
      changelog: ${{ steps.changelog.outputs.changelog }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Generate changelog from commits
        id: changelog
        run: |
          # Nearest version tag that is an ANCESTOR of this one (git describe
          # walks ancestry, so a tag on a squash-merged side branch — not in
          # this commit's history — is correctly skipped). `--match 'v[0-9]*'`
          # keeps any non-version tag from being picked as the baseline.
          PREV_TAG=$(git describe --tags --abbrev=0 --match 'v[0-9]*' HEAD^ 2>/dev/null || echo "")

          if [ -z "$PREV_TAG" ]; then
            # No ancestor tag (genuine first release, or earlier tags live on
            # squash-merged branches outside this history). Do NOT fall back to
            # `git log HEAD` — that dumps the entire repo history into the notes.
            # A short note is the honest, readable result.
            COMMITS="- Initial tagged release from this history."
          else
            # Commits since the previous ancestor tag.
            COMMITS=$(git log --pretty=format:"- %s" "${PREV_TAG}..HEAD")
          fi

          # Categorize commits
          FEATURES=$(echo "$COMMITS" | grep "^- feat" || true)
          FIXES=$(echo "$COMMITS" | grep "^- fix" || true)
          DOCS=$(echo "$COMMITS" | grep "^- docs" || true)
          REFACTORS=$(echo "$COMMITS" | grep "^- refactor" || true)
          OTHER=$(echo "$COMMITS" | grep -v "^- feat\|^- fix\|^- docs\|^- refactor" || true)

          # Build changelog. Category headers are h3 (###) so they nest under
          # the release body's "## What's new" h2 instead of colliding with it.
          CHANGELOG=""

          if [ -n "$FEATURES" ]; then
            CHANGELOG="${CHANGELOG}### Features\n\n${FEATURES}\n\n"
          fi

          if [ -n "$FIXES" ]; then
            CHANGELOG="${CHANGELOG}### Bug Fixes\n\n${FIXES}\n\n"
          fi

          if [ -n "$DOCS" ]; then
            CHANGELOG="${CHANGELOG}### Documentation\n\n${DOCS}\n\n"
          fi

          if [ -n "$REFACTORS" ]; then
            CHANGELOG="${CHANGELOG}### Refactoring\n\n${REFACTORS}\n\n"
          fi

          if [ -n "$OTHER" ]; then
            CHANGELOG="${CHANGELOG}### Other Changes\n\n${OTHER}\n\n"
          fi

          # Output for GitHub Actions
          echo "changelog<<EOF" >> $GITHUB_OUTPUT
          echo -e "$CHANGELOG" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

  # ============================================================================
  # CREATE RELEASE
  # ============================================================================
  release:
    name: Create Release
    needs: [sbom, changelog]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Download SBOM
        uses: actions/download-artifact@v8
        continue-on-error: true
        with:
          name: sbom
          path: ./artifacts

      - name: Extract version
        id: version
        run: echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Create Release
        uses: softprops/action-gh-release@v3
        with:
          name: CortenForge ${{ steps.version.outputs.version }}
          body: |
            # CortenForge ${{ steps.version.outputs.version }}

            The CortenForge SDK — a Rust workspace for mechatronics design and
            simulation — and **Cendrillon**, the guided app that takes a 3D scan
            all the way to printable, silicone-cast molds.

            ## Running it

            There is no download; everything here is built from source.

            You need a Rust toolchain (see `rust-toolchain.toml`), **CMake and a
            C++ compiler** — `manifold-csg-sys` builds a vendored C++ kernel
            through CMake — and a working Vulkan or GL driver for the 3D views.
            On Debian/Ubuntu that is:

            ```
            sudo apt-get update
            sudo apt-get install cmake build-essential \
              libwayland-dev libxkbcommon-dev libasound2-dev \
              libudev-dev libfontconfig1-dev
            ```

            ⚠ That list is duplicated on cortenforge.com/cendrillon/ and nothing
            enforces that the two stay in step — change both together.

            Then:

            ```
            git clone https://github.com/via-balaena/CortenForge.git
            cd CortenForge
            git checkout ${{ steps.version.outputs.version }}
            cargo run -p cf-studio-gui      # Cendrillon
            ```

            The `git checkout` matters: these notes are attached to this tag,
            and a download used to deliver exactly this tag's code. Without it
            you build whatever `main` is when you read this. Omit it if you
            want the newest work instead.

            Building from source also removes the unsigned-binary warnings the
            old downloads carried on macOS and Windows.

            ## What's new

            ${{ needs.changelog.outputs.changelog }}

            ---
            By using this software you accept the terms in `DISCLAIMER.md` — it is general-purpose research software, provided **as is**, **not a medical device**, used **entirely at your own risk**. The repository carries `LICENSE-APACHE`, `LICENSE-MIT`, `NOTICE`, and `DISCLAIMER.md`.

            A CycloneDX SBOM for `cf-studio-gui` is attached — Cendrillon's own resolved dependency graph, not the whole workspace, whose ~280 per-crate SBOMs would trip GitHub's upload limit. Read the file for its contents. The workspace meets the A-grade quality standard ([STANDARDS.md](https://github.com/via-balaena/CortenForge/blob/main/docs/STANDARDS.md)).
          files: |
            artifacts/*.cdx.json
          fail_on_unmatched_files: false
          draft: false
          prerelease: ${{ contains(steps.version.outputs.version, '-') }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # ============================================================================
  # PUBLISH TO CRATES.IO (Optional - manual trigger)
  # ============================================================================
  # publish:
  #   name: Publish to crates.io
  #   needs: [release]
  #   runs-on: ubuntu-latest
  #   if: ${{ !contains(github.ref, '-') }}  # Only stable releases
  #   steps:
  #     - uses: actions/checkout@v7
  #
  #     - name: Install Rust
  #       uses: dtolnay/rust-toolchain@stable
  #
  #     - name: Publish crates
  #       run: |
  #         # Publish in dependency order
  #         cargo publish -p mesh-types --token ${{ secrets.CARGO_TOKEN }}
  #         cargo publish -p mesh-io --token ${{ secrets.CARGO_TOKEN }}
  #         # ... etc
  #       env:
  #         CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_TOKEN }}
